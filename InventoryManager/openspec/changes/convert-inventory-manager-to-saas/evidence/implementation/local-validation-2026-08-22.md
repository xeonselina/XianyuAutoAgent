# SaaS Core local implementation validation — 2026-08-22

## Scope and identity

- Branch: `saas-main`
- Starting commit: `53193b6724693132be13cd084101e9cd62142c63`
- Validation ran in the existing shared dirty worktree. Unrelated user changes were preserved.
- This record covers a substantial local implementation slice; it is not a declaration that the project or production migration is complete.
- No `project_complete_at = T` or `earliest_rehearsal_at` was recorded because tasks 12.2–12.13 and their direct dependencies are not complete.

## Database and side-effect boundary

- The test harness rejects production writes and keeps real-database mutation/contention tests behind explicit opt-in gates. No write, provider or print gate was enabled for the final commands.
- The latest addendum connected only to `inventory_management_test` through its exact schema-only platform-read identity to capture a consistent read-only source baseline. It performed no DML/DDL, locking read, real provider request or physical print and never connected to the production business schema.
- The harness permits production only for separately enabled, grants-proven, non-locking read-only observation. All writes, DDL, fixture resets, migrations, and destructive injection require explicit real-test authorization and an exact target database name of `inventory_management_test`; schema drift fails closed.
- Real MySQL contention tests remained skipped under this offline boundary.

## Implemented local slices

- Control-plane migrations and domain services for foundations, crypto/root-key handling, identity/session/RBAC, invitations, SMS challenges, registration publication, subscriptions/redemption, lifecycle, recovery, jobs/outbox, integrations, fleet/schema operations, backups, health and monitoring signals.
- Trusted tenant routing, route/account mutation, engine-cache invalidation, database identity and tenant HTTP boundary primitives.
- Tenant-schema expand migrations and local services for multi-warehouse data, device/inspection warehouse facts, logical accessory inventory, rental logistics facts, shipping execution ledgers and express-type backfill.
- Gantt SaaS proof composition, session-bound preview authority, current-generation fencing, fail-closed HTTP runtime and shared D33 schedule-overlap consumers. The normalized `/api/gantt/view` now returns flat device/rental DTOs, shallow device-bound/logical-accessory summaries without logical-unit IDs, one-snapshot range statistics, unfiltered model facets, summary revisions and D33 warnings. Its 100-device/31-day regression uses six fixed business SQL statements plus constant tenant-identity verification, including the two accessory batch projections, and remains below the eight-business-SQL recommendation.
- Shared tenant-business HTTP runtime for control-database UTC authentication, effective-gate/CSRF/RBAC evaluation, canonical trusted tenant timezone, trusted DML routing and one independently bound tenant session per request. It now has its own composition root rather than depending on Gantt installation; Gantt reordering consumes the same scope without changing its proof or fence behavior.
- The application factory now has an explicit opt-in SaaS Core HTTP composition root for the migrated shared tenant, tenant Identity, platform Identity, Subscription, Gantt, Rental, Inspection, Warehouse and Relay runtimes. Deployment must supply a typed database-instance registry, absolute root-key directory and bounded pool/cache settings alongside the independently initialized control database. Tenant Identity and Subscription are forced to share the exact control database, tenant HTTP boundary and session service used by the business runtime; platform Identity uses a distinct Cookie/CSRF/session service while sharing only the control database. The complete object graph is built without connecting and all nine extensions are published in one update; a missing/invalid/partial graph publishes none, and an unconfigured process retains the existing fixed-503 boundary instead of inferring authority from the legacy Flask database.
- The tenant browser identity slice now exposes a control-only no-store session-status endpoint and a CSRF-protected logout endpoint. Status resolves the opaque fixed host Cookie against current control-database identity, membership, access-version and effective-gate facts without opening a tenant database. Logout uses the same authenticated session and database UTC instant to revoke itself atomically, then expires the fixed Cookie. Active, expired and suspended tenants retain logout; absent composition returns a fixed 503 and invalid credentials return the same fixed 401 without echoing bearer material.
- The same identity runtime now lists at most 100 current devices owned by the caller using a fixed safe projection of session UUID, bounded device summary, created/last-seen times and current-device marker; token/CSRF digests and IP fields are never selected or returned. CSRF-protected specified-device revocation authenticates before parsing the UUID, gives malformed and cross-user targets the same fixed response, and is idempotent without changing global auth version. Revoke-all increments the current user's auth version and revokes every active session in one control transaction. Desktop and mobile account-security pages consume only this contract, keep pending-warehouse account security reachable, and discard their tab-scoped CSRF value when the current/all-device action ends the session.
- Tenant OTP login now has an executable injected-provider boundary. Canonical +86 normalization precedes lookup/rate limiting/provider dispatch; purpose-bound challenges use the platform root-key registry, exact login context and existing D37 control-database limits. The reservation transaction commits before provider invocation and delivery outcome is recorded in a second short transaction. The final transaction consumes the challenge, current-reads user/membership/tenant, activates only the default-migration unverified Admin exception, optionally verifies its phone, issues a brand-new opaque session and rotates an old same-user Cookie. One control migration adds unique challenge/session rotation anchors; a following migration adds immutable minimal tenant-auth security events. Login create/rotation, logout, specified-device revoke and revoke-all append their event in the same control transaction without token, digest, phone, IP or arbitrary payload, and idempotent revoke replay emits no duplicate. Token and CSRF digest uniqueness collisions retry within nested savepoints without rolling back caller work. Isolated SQLite upgrade/downgrade plus metadata comparison pass. No session-duration product default was invented: login remains disabled unless deployment supplies an explicit typed idle/absolute/version policy, provider and trusted-source resolver. The current provider tests use only an in-process fake; production Tencent Cloud adapter/readiness remains open.
- Pending invitation expiry now has an executable bounded background sweep over the existing D47 persistence service. It selects only database-clock-due pending identities/revisions, requires an explicit 1–1000 batch size, and expires each invitation in its own short transaction through the existing canonical-user → invitations → tenant → seat-guard → challenge → membership lock order. Quota availability already current-reads deadlines, so a delayed sweep cannot retain a seat; sweep crash/restart or a concurrent revision change only causes a safe current rescan and never revives the old token generation.
- Platform administrator authentication now has an executable control-only login and HTTP boundary. Password verification uses the existing versioned memory-hard hash, then the same transaction must accept a non-replayed current TOTP step or consume one current-generation recovery code before issuing a fresh opaque platform bearer and independent CSRF proof. Versioned password/MFA failure policies must explicitly cover username, trusted source IP and platform device; the database stores only root-key-purpose-derived subject digests and fixed low-cardinality counters. Current TOTP credentials resolve their recorded active/legacy root-key version while new rate subjects use the active key. Unknown account, bad password, bad/replayed factor and throttle rejection share one public response. A failed immutable audit append rolls back factor consumption and session issuance. The independent host-only Secure/HttpOnly/SameSite=Lax Cookies use `/platform`, cannot authorize tenant routes, and `/platform/api/login|session|logout` is installed in the atomic SaaS runtime bundle. Login and logout write credential-free `platform_audit_logs`; its isolated migration round trip and metadata comparison pass. CLI bootstrap/reset/disable, setup-only UI, platform session management, factor replacement and action step-up are recorded in the later slices below.
- The trusted-source resolver now also has a concrete explicit-policy implementation. Deployment must supply at least one canonical trusted-proxy CIDR and a bounded forwarded-address limit; there are no built-in production networks or hop counts. An untrusted direct peer cannot override its source with `X-Forwarded-For`; a trusted peer walks the chain from the right across only configured proxy networks. Missing, malformed, ambiguous or oversized trusted-proxy input becomes the single conservative `unknown` rate-limit bucket. Direct SMS-package import and lazy login exports are covered so this adapter does not reintroduce the earlier identity/SMS package cycle.
- A production-shaped Tencent Cloud adapter now follows the official SMS API `v20210111` fields and uses the product-scoped official Python SDK dependency. SmsSdkAppId, approved sign/template, region, request timeout and template-variable order are all explicit typed deployment inputs; Secret ID/key are accepted only by the builder and are absent from settings/repr. The adapter sends one canonical E.164 number, consumes the plaintext code once, maps one `Ok` status to sent, an explicit provider rejection to failed, and missing/malformed response or raised transport ambiguity to send_unknown through the outer runtime. If the approved template includes expiry minutes, construction verifies that its value exactly matches the challenge policy. All adapter tests use a fake SDK client; no Tencent endpoint was contacted, and production qualification/readiness is not claimed.
- Desktop and mobile now provide OTP login, restricted tenant-status and account-security routes. Challenge/code values remain component memory; successful verification stores only the independent CSRF proof in tab-scoped sessionStorage while the bearer stays in the Secure/HttpOnly Cookie. Active login flows through the authoritative warehouse setup guard; expired/suspended login reaches one restricted shell, and only suspended Admin exposes own account security.
- Tenant subscription renewal now has a separate control-only runtime and shares the exact control database and tenant HTTP boundary used by Identity. The expired-page status endpoint projects only effective expiry, expiry time, subscription revision and whether the actor can redeem. The CSRF-protected redemption endpoint authorizes before code parsing, prelocks tenant/current recovery run, and delegates the final current-read transaction through run, hold, unresolved deletion, unresolved suspension, code, Admin membership and subscription in fixed order. It uses the consumed code's frozen terms, performs `max(database now, current expiry) + exact duration`, atomically consumes the code, updates active/expired projections and appends one immutable event. Idempotent replay returns the original result; malformed and unknown codes share one public response; a suspension that wins after session precheck leaves code/subscription/event unchanged. Desktop and mobile expired pages expose the form only to Admin, keep code in component memory, and reread authoritative session status after success or an ambiguous response before restoring business navigation.
- Natural subscription expiry now has a durable control-plane execution boundary rather than depending on Web APScheduler or ordinary tenant-business authorization. A bounded scheduler finds only database-clock-due drift, then locks each tenant in its own transaction through current recovery run/hold, active deletion, active suspension/current action and subscription before enqueuing a revision-keyed `subscription_projection_evaluate` job. Its dedicated authority repeats that prefix before the job-row lock and deliberately permits the provider-free reconciliation under expired or higher lifecycle overlays; the handler current-reads the same prefix again and idempotently reconciles only subscription plus active/expired tenant projection. A renewal or access-version change after enqueue is therefore re-evaluated rather than overwritten, suspended/deletion projections are preserved, and a crash after the projection commit can safely retry. Missing current recovery run/hold or a corrupt suspension/action pair fails closed. Batch size, priority and bounded attempts are explicit operator inputs; no scheduling interval was invented in application code. After a complete sweep returns to the control boundary, the scheduler writes the existing versioned `evaluator.heartbeat` through a required injected recorder in a separate transaction; a queue/database failure emits no green heartbeat, while per-tenant authority skips remain visible in the sweep result. The generic durable worker now likewise requires an injected recorder and writes `worker.heartbeat` only after a successful/review/blocked/idle loop returns; an unhandled claim/runtime failure produces no healthy observation.
- Rental HTTP migration boundary: every legacy global-session Rental route is blocked before its handler in non-test processes; the API/Web rental-detail aliases, paginated list/search, and paginated pending-return/due-today aliases now use the explicit runtime with `RENTAL_READ` plus `CUSTOMER_PII_READ`. Search validates bounded inputs after authority and checks CSRF for its existing POST contract. Detail, list/search, and pending-return responses use fixed scalar projections rather than ORM relationship traversal; 100-row list/search and one-record detail regressions each perform exactly two SQL statements. Detail overdue state and pending-return dates derive from control-database UTC plus the authenticated tenant timezone. The edit-context combines the authoritative detail, logical accessory request fulfillment projection, booking metadata, active/current main-device choices and transition-only legacy device-bound accessory facts in one tenant transaction and seven fixed business reads; it never exposes logical-unit IDs. Both desktop and mobile edit pages now initialize from that single request, and the desktop's duplicate rental/model watcher fan-out is replaced by one open-or-ID watcher. Booking bootstrap returns warehouses, the actor's recent/default warehouse, device models, accessory types and form policy in three projected reads. Booking availability performs one batch evaluation over candidate devices, warehouse origins, D33 schedules and logical-accessory facts; its two-warehouse, 100-device, 31-day regression stays at five business SQL statements. Until a trusted SF duration adapter is installed, each warehouse returns an explicit safe failure and a context-bound zero-to-seven-day manual-confirmation path rather than a fixed origin or default duration. Production `POST /api/rentals` and both `PUT /api/rentals/{id}` / `PUT /web/rentals/{id}` aliases now enter `RENTAL_WRITE` on the explicit tenant runtime instead of the global session. Their final transactions lock and recheck the selected/current devices and warehouses, rerun availability with self-exclusion and context-bound logistics, persist structured destination/estimate facts and damage-note edits, and use the logical-accessory inventory service for ordinary request/link allocation or reallocation. Origin movement and hard usage-period/accessory contention return stable 409 results. Relay-confirmation and confirmed relay-shortage paths persist an unfulfilled request plus a non-blocking warning; an update that would alter an existing relay-sourced or requestless travel link returns stable `ACCESSORY_CHAIN_RECALCULATION_REQUIRED` and rolls back until the complete chain solver is integrated. No response exposes unit IDs. Missing/malformed composition still returns a fixed no-store 503, and all not-yet-migrated write/provider routes remain unavailable rather than falling back.
- Both desktop and mobile Gantt stores now consume `/api/gantt/view`; the page header consumes `daily_stats_by_date`, model/lifecycle filter changes issue one aggregate refresh, stale overlapping responses cannot overwrite the latest generation, the desktop `statsCache` and desktop/mobile per-day HTTP fan-out are removed, and store mutations no longer perform hidden refreshes. Each page success coordinator owns the single current-window refresh. Pending-return count comes from the range summary and the full list is loaded only when the drawer opens. Automatic Xianyu provider refresh on Gantt mount was removed; the explicit user refresh remains. Both create clients load one booking bootstrap rather than device/model/physical-accessory lists. Both edit clients load one edit-context and no longer offer physical phone-holder/tripod IDs. All four clients debounce stable structured-address/date/model/preferred-warehouse/accessory-type changes into one availability request, fence stale responses, support context-bound manual logistics confirmation, select logical accessory types rather than physical IDs, and submit the final tenant-runtime contract. Edit payload regressions assert the structured destination and `exclude_rental_id` and reject reintroduction of `accessories` or inline `status` mutation fields.
- Rental final-write usage-period guard for create, update and status transitions with stable `USAGE_PERIOD_CONFLICT` HTTP 409 results, Device-to-Rental lock ordering, explicit child-row locking, and parent/child logistics/status synchronization.
- Rental status and safe-delete mutations now use the same trusted tenant runtime. The status transaction enforces a fixed transition matrix, locks logical-accessory requests/links, refuses ordinary dispatch until every request is fulfilled, writes `dispatched` custody and authoritative shipped/returned timestamps atomically, and refuses completion while any logical unit is still held by the rental. The pending-return client removes its local row after success and leaves the page-level coordinator as the sole Gantt refresh owner. Safe delete rejects shipped/returned rentals, relay cases, outbound shipment history, current holders, relay-sourced links, and requestless travel links; an eligible ordinary rental releases its links, requests, legacy children, and main row in one transaction. Both mutations return compact DTOs without logical-unit identifiers and preserve the testing-only legacy compatibility boundary.
- The same-device logical-accessory relay planner now locks the device, main rentals, downstream relay cases, logical types, units, requests and links in a fixed sequence and derives the whole affected chain before writing. An agreed edge propagates every travelling unit through requestless intermediate rentals; a late upstream agreement replaces ordinary unshipped downstream links, while a pre-handoff downgrade removes unreachable links and deterministically reallocates each remaining request from the device warehouse or returns a non-blocking type-level shortage. Replacements append immutable idempotent `unlinked/linked` facts without exposing unit IDs. The routed tenant-session mutation service now also exposes an internal, provider-free external-stage projection for a future durable execution/reconciliation worker: under the caller's tenant transaction it recomputes an agreed edge, verifies exact predecessor custody, appends one `relay_handoff`, advances successor/tracking/status/milestones, or projects `completed`; it never resolves SF/Xianyu/printing or opens a control connection. The public HTTP runtime still rejects `shipped/completed` with 503 until orchestration exists. A new A→B→C regression agrees B→C first, proves upstream agreement fills both neutral links, rejects the premature downstream handoff, executes both edges in order, then replays A→B after custody has legitimately advanced to C. That replay now trusts the exact immutable event rather than requiring current custody to remain frozen at B, so it neither fails nor rewinds the holder. Injected audit persistence failure rolls back case status, tracking, successor shipment state, holder and event together. D41 inspection still prelocks its complete known device/rental scope before warehouse rows, retains only the reachable same-device agreed chain for a normal receipt, and reassigns every other future request from the main device's current ready warehouse or leaves the request as an explicit shortage. Damaged/lost units are never reused, a future fulfillment execution freezes the whole mutation, and the public result lists only affected/shortage rental IDs plus type-level counts.
- The complete inspection route surface now uses a separate protocol-complete tenant runtime in every non-test process; the legacy global-session implementation requires a literal testing-only flag. Latest-by-device/name, detail and paginated list use minimized projected DTOs and tenant-local business dates. The latest context returns ready warehouses, the actor's remembered/default inspection warehouse, checklist facts, and type-level logical-accessory receipts without unit IDs. First inspection creation requires a returned main rental and an exact receipt result for every unit currently held by it; the same transaction creates the record/items, moves the main device, appends device movement history, remembers the actor's inspection warehouse, applies each logical-unit receipt, and performs the D41 future-link repair described above. Normal receipt clears the holder and makes the unit active at the actual warehouse, damaged receipt clears the holder into `maintenance`, and missing receipt retains the holder and marks the unit `lost`; immutable `inspected`, `warehouse_moved`, `maintenance`, `lost`, `unlinked`, and replacement `linked` facts are appended as applicable. The creation response and desktop success/warning message list affected and still-short rental IDs without exposing unit IDs.
- Warehouse list/create, active main-device list and explicit device-move preview/confirm now use a fifth tenant HTTP runtime rather than the Flask global session. The underlying operation accepts an explicitly routed Session and caller-owned transaction while retaining the existing single-tenant compatibility wrapper for isolated legacy tests. Preview derives the tenant business date from trusted control UTC/timezone, returns the affected order/date/logistics/accessory summary and a complete fact revision without unit IDs, and uses `WAREHOUSE_DEVICE_MOVE` plus read capabilities. Confirmation uses the authenticated actor, requires the original warehouse/revision/explicit confirmation, locks device → affected rentals → source/target warehouses → requests/links/units, and atomically writes real device location, movement history, ordinary future-link reallocation and visible shortage facts without changing rental logistics fields. Manual moves and inspection moves now share `DeviceWarehouseLocationService`, so both mutate the locked device and append the source-labelled movement record through one caller-owned transaction primitive. Missing composition fails closed with a no-store 503. A desktop Warehouse Management view consumes these endpoints, shows the immutable logistics warning and affected-order table before confirmation, and reports any resulting shortage rental IDs after commit.
- Every normal shared tenant-business scope now performs one fixed preflight read for exactly one `active+ready` default warehouse; pending, inactive, absent or inconsistent defaults return `tenant_setup_required` before a business transaction is yielded. The preflight rolls back its read-only AUTOBEGIN instead of committing. Only the Admin-only `WAREHOUSE_SETUP` capability can open the dedicated default setup read/write path while pending. Warehouse management now also provides edit, unique-default transfer, history-preserving deactivation and per-user booking/shipping/inspection preferences. Deactivation rejects the current default and warehouses containing active serialized devices or non-retired logical units. Main-device creation resolves an active tenant-local main model and an explicit ready warehouse or the sole default in the same caller-owned transaction. Both desktop and mobile setup pages confirm all provisioned default fields; the desktop management page exposes edit/default/deactivate, recent-work-warehouse and main-device creation controls. Both routers now probe that authenticated setup endpoint before navigation: a confirmed pending tenant is sent to setup, a ready tenant cannot re-enter setup, and an unavailable/unauthorized probe is never reinterpreted client-side. The remaining login/tenant shell and retirement of the legacy global device CRUD remain explicitly assigned to tasks 10.2/10.7.
- Relay now has its own trusted tenant runtime for reads and provider-free writes. Case lists and manual-pair options use the routed tenant Session and tenant-local business date/timezone, parse bounded query inputs only after `RENTAL_READ` plus Relay/inventory/PII authorization, and return private no-store responses. Manual pairing and `pending/notified/agreed` status changes use the authenticated actor and control-database time inside a caller-owned tenant transaction. They lock device, all main rentals, cases/bindings and then logical units in a stable prefix, revalidate the shared D33 candidate on automatic agreement, and atomically compose whole-chain recalculation on agreement or rollback. D34 shortage remains a request without a link and a type-level non-blocking result; desktop and mobile status controls persist an optional internal 500-character supplemental note without creating a second shipment, waybill, print or tracking fact. In non-test processes `shipped/completed` mutations and provider-backed tracking still fail closed with a stable 503 instead of reaching the legacy Session or provider. The testing compatibility flag is literal-test-only. Durable outbox/provider execution and reconciliation are still required before those external stages can migrate.
- D55's generic API-key surface is retired: no `/external-api` route is registered, an injected legacy `X-API-Key` or `API_KEY` has no authority, and the current runtime/model/migration tree has no tenant API-key facility. Operational environment, Docker, Windows/Makefile and startup-document templates no longer request `API_KEY`. SaaS Gantt preview/execute already uses the platform-root-derived tenant/session/authority proof. Generic `SECRET_KEY` is now absent from normal and testing application configuration plus those operational templates. The isolated legacy Gantt transaction regressions use a separately named random test-only signing key behind both literal Flask testing mode and the legacy-test flag; a non-test process rejects that signer even if the test key is injected. Negative tests retain the old names solely to prove they cannot establish identity or preview authority.
- Default-tenant migration manifest, strict serialization, phase ordering, reconciliation evidence, and a crash-safe local journal with advisory lock/CAS and atomic persistence. The `run-phase` command now derives one stable execution key per immutable manifest/phase, never invokes an executor during dry-run, requires an explicitly injected executor for apply, records only one immutable completion transition, replays durable evidence after response loss, and resumes a pre-journal crash with the same key. Ordered phase composition derives stable per-step keys and appends no phase evidence after a partial/crashed step sequence. Backfill composition runs all declared mutation steps followed by exact-key reconciliation and returns evidence only for a passed fully covered versioned policy/report. The explicit authoritative-write command persists a one-way UTC marker only after database/jobs evidence and makes legacy rollback unavailable.
- D60 default-tenant grant integration now derives tenant/database/baseline/idempotency/plan identities only from the immutable manifest, refuses to run before durable backfill reconciliation, and writes inside the caller-owned final control transaction. Existing ledger idempotency uses database current UTC, exactly `3,153,600,000` seconds, an immutable `migration_grant` event, and stable tenant/database/baseline/key identity; isolated tests prove a retry 30 days later returns the original subscription/event/expiry without another write.
- Default-tenant in-place registration now has two explicit caller-transaction operations for the unavoidable cross-database boundary. The already selected original business schema creates or exactly replays its one `database_identity`; the control transaction then creates or exactly replays the manifest-bound tenant, first unverified Admin, migration Admin membership, non-secret route metadata and control-side identity record. Controlled display/phone inputs are commitment-checked before control reads/writes, partial or conflicting existing identities fail closed, DML/read account metadata must be distinct, and no API accepts a DSN, password or provider secret. The expand route remains `provisional` until a later application-enforce publisher is implemented.
- Default-tenant backfill now includes default warehouse/device placement, canonical express-type, planned logistics, explicit structured rental addresses, logical-accessory conversion, and provider metadata ownership slices. Planned logistics binds the complete expected main/child set and shared D33 result; partial legacy facts or source drift roll back the batch. Structured addresses require a reviewed per-row mapping bound to commitments of the exact legacy `destination` and target fields, never infer province/city/district from free text, retain the legacy display field, and roll back the batch on source/relationship/partial-target drift. Logical accessories use an explicit device/type/warehouse plan rather than name inference, require every unfinished accessory child rental, create stable legacy-source units/requests/reliable links and migration events, reconstruct current holder only for reliable shipped/returned facts, and keep uncertain units unavailable. Provider metadata accepts only an empty config and has no credential parameter, so SF/Kuaimai/Xianyu rows remain `unconfigured` with zero revision/current pointer/claim until a separately submitted rotated credential is verified.
- Concrete default-migration phase adapters now bind those resolved transforms to fresh caller-owned control/tenant Sessions. Expand composes explicit control-schema and tenant-schema steps before crash-resumable in-place registration; a crash after tenant `database_identity` commit but before control registration safely replays the first half. Backfill runs warehouse, express type, planned logistics, structured address, logical accessory and control integration metadata in declared order, rolls back the failing step, and emits replay-stable step evidence after committed idempotent retries. Integration evidence rejects any secret-revision pointer. The complete builder refuses to exist without an explicit `historical_snapshots` step, so the unresolved legacy-credential ownership rule cannot be replaced with a placeholder.
- Reconciliation now has deterministic exact-key collector orchestration plus a SQLAlchemy scalar adapter that rejects locking SELECTs and Sessions carrying pending writes, disables autoflush, never commits, and converts adapter/data-shape failures to fixed safe errors. Its versioned default-tenant policy and concrete registry query device rows/warehouse links, rental amount/device links, valid logical-accessory links, core foreign-key orphans, historical waybills, tenant credential revisions and the unique default warehouse from already-bound tenant/control Sessions. Schema generation/digest reuse one trusted fleet post-DDL inventory observation. The registry requires an independent `legacy.double_count` authority-boundary collector instead of pretending that database row presence proves old readers are disabled, and exact 12-key composition fails before reads on any gap or duplicate. The complete isolated-snapshot command composition remains pending.
- The two fixed public health endpoints now have executable independent small budgets rather than placeholder admission booleans. A non-blocking semaphore lease is held across each endpoint's minimal control read and released afterward; a separate fixed-window budget rejects excess probes before a control connection checkout. Existing fixed no-store bodies, query-parameter rejection, marker-bound host-restore completion, heartbeat/delivery-latch separation, zero tenant traversal, and error-detail suppression remain covered by endpoint tests.
- Operational freshness now has an independent bounded evaluator over deployment-explicit low-cardinality signal keys. It advances each initialized signal in its own short control transaction using database UTC, preserves the existing versioned hysteresis/repeat/recovery reducer, reports missing rows without blocking other signals, and does not duplicate lifecycle events when the same evaluation instant is replayed. `notification.delivery` is rejected from this periodic list because it is an event-driven failure latch whose healthy idle period must not become stale. The remaining invocation cadence and Tencent notification dispatcher stay deployment/operations work rather than implicit code defaults.
- Durable queue state now has a two-signal low-cardinality adapter with no tenant/job dimensions. Deployment explicitly supplies the oldest-due-wait threshold, terminal-failure lookback and terminal-failure-count threshold. One database-time snapshot excludes future pending work and failures outside the window, treats an empty queue as healthy, and atomically records `queue.oldest_wait` before `queue.consecutive_failures`; failure of the second write rolls back the first. Counts and wait seconds are returned only as a bounded operator snapshot and are not persisted as an unbounded metrics series.
- D23 backup retention now has both a deterministic control-side plan and a fail-closed NAS filesystem executor. The fixed policy selects the latest successful recovery point in each of 48 hourly, 30 daily and 12 monthly buckets in an explicit timezone and unions those tiers. Cleanup is accepted only when the trigger is the newest verified completion. The NAS executor shares the pull lock, re-verifies the trigger and every present candidate's canonical manifest, identity, checksum and size, preflights the entire plan before unlinking, then removes the artifact completion marker before its manifest with directory fsync. Exact replay cleans a validated manifest-only crash orphan or reports an already absent pair without touching retained sets. It never computes policy, queries a database, acknowledges a backup or invokes cloud sync; real scheduling remains 11.4/11.6 work.
- Platform administrator identity now has an independent control-only namespace from host bootstrap through browser session management. A dependency-injected host CLI adapter creates a pending administrator or performs an audited break-glass reset without accepting a password, TOTP seed, recovery code, DSN or provider secret; reset revokes all existing setup, factor and session authority before returning a new one-time setup token. The setup-only HTTP flow consumes that token, sets the password, returns a newly encrypted active-root TOTP seed once, confirms it against the credential's recorded root-key version, returns one generation of recovery codes once and activates the account. Platform login requires password plus current TOTP or one recovery code in one transaction, applies explicit username/source/device rate policies to both stages, writes a credential-free immutable audit record and only then issues the host-only `/platform` bearer and separate CSRF. Self-service session projection, specified revocation and revoke-all expose no token, digest or IP, authenticate before parsing a target UUID, and atomically audit real revocations. Recent step-up is implemented as the separately recorded D52/D58 session rotation primitive; the host launcher, credential replacement, last-active-Admin-safe disable and D53 factor proof are implemented, while real deployment injection/smoke remains an operations prerequisite.
- The desktop platform identity UI consumes only that independent namespace. `/platform/setup` keeps the host-issued setup token, new password, TOTP seed/code and recovery codes in component memory, clears earlier-stage material immediately and refuses to leave the one-time recovery display until the operator acknowledges offline storage. `/platform/login` clears password and factor after every attempt, stores only the platform-specific CSRF proof in tab session state and relies on the HttpOnly `/platform` Cookie for bearer authority. `/platform/security` loads only the fixed self-session projection and supports current, specified and all-session revocation. The tenant warehouse guard never probes tenant authority for a `/platform` route. No tenant CSRF, Cookie or API is used as platform authority.
- D52/D58 recent MFA now has a concrete platform-session rotation primitive and HTTP/UI entrypoint. It requires the current platform bearer and matching platform CSRF, applies the explicit MFA username/source/device failure policy, accepts only a globally fresh TOTP time step or one current recovery code, and in one control transaction writes replay protection, creates a fresh bearer/CSRF, revokes the old session and appends a credential-free audit record. The replacement session never extends the old absolute-expiry ceiling. A rejected factor keeps the current session usable and stores no presented credential; D53 remains a separate per-action factor transaction and does not consume or update this reusable recent proof.
- Logged-in platform factor management now reuses the same current-factor verifier and MFA rate-limit subjects as login and recent step-up instead of maintaining a third verification branch. A current TOTP or recovery code may stage a new encrypted TOTP seed while the old credential remains confirmed. Only a valid code from that staged seed atomically retires the old credential, promotes the new generation, revokes the old recovery-code generation, returns a new one-time batch, advances `auth_version`, and revokes all old platform sessions. Recovery codes can also be regenerated independently under a fresh current factor. A bad confirmation leaves the old TOTP and current session usable. The desktop security page keeps seeds, factor inputs, and one-time recovery codes in component memory and clears them on teardown; successful TOTP replacement clears platform CSRF and requires an explicit return to login after the operator saves the new codes.
- D53 service-period adjustment now has one reusable domain calculator/metadata normalizer, one tenant-first lifecycle gate repository and a purpose-separated signed-confirmation codec shared with the existing proof token primitives. Preview accepts only add-positive-days, subtract-positive-days or expire-now, uses the control database clock, never accepts a target expiry, and binds the normalized request plus tenant/access/subscription, recovery, deletion and suspension revisions into a five-minute action confirmation. Commit verifies actor/session/auth version and every fence before consuming a factor, then locks tenant first and atomically writes the fresh TOTP/recovery consumption, subscription CAS, one immutable event and one success platform audit. A failed factor rolls the action back and records only fixed MFA counters/audit in a separate short transaction; stale lifecycle state does not consume the factor, and exact response-loss replay returns the existing event without duplicate factor or audit. The desktop route invalidates confirmation on any input change, keeps confirmation/factor in component memory, formats the preview in Asia/Shanghai and repeats the non-refund disclaimer.
- Platform redemption-code management now composes against the same independent platform boundary and shared credential-free control-audit constructor instead of adding another authentication or audit branch. The domain management service uses fixed column projections for masked list pages, effective deadline status filtering, record-version-fenced revocation and active/legacy root-key reveal. Generation requires the current completed recovery run and the sole active Core plan, reuses the existing CSPRNG/canonicalization/envelope service, persists bounded channel/internal-note metadata in the idempotency digest, and commits code rows plus generation/initial-export audits before returning one in-memory CSV; an exact request replay creates nothing and returns no plaintext export. Authorized input denials, state conflicts and missing resources append credential-free rejected audits through one reusable operation map. Individual reveal uses the shared versioned control-database limiter with a separate `code_reveal` scope and deployment-owned username/IP/device thresholds; successful and rejected authorized attempts consume the same stable-order buckets, while an already-blocked attempt commits a credential-free `rate_limited` audit without decrypting. The fixed list projection now joins only the unique source lineage and unresolved integrity incident to show `issued` or `integrity_blocked`, without janitor/outbox, provisional-resource or identity details. The desktop page downloads the first CSV immediately, never offers historical bulk export, clears individually revealed codes on close/unmount and exposes no registration retry, abandon or cleanup action. Task 6.2 is closed; the actual D54 replacement transaction remains separately open under 6.8/6.9.
- The Gantt store now exposes one computed status index and reusable `getRentalsByStatus` selector. The performance suite measures that product selector instead of rerunning 500-item Vue Proxy traversal 1,000 times; this removes host-load flakiness without relaxing the 500 ms threshold and gives later status consumers one shared lookup path.
- Platform-admin deactivation now uses the same host-only CLI boundary as bootstrap and credential recovery. The service locks the administrator set before deciding eligibility and counts a successor only when it is `active` with a password, one current confirmed TOTP, and current-generation active recovery codes. It then reuses one authority-revocation helper for setup challenges, TOTP credentials, recovery codes, and sessions, advances the target's auth version, marks it disabled, and appends one credential-free CLI audit row in the same transaction. Attempting to disable the sole active administrator leaves all authority unchanged.
- The production host launcher is now executable as `python -m inventory_control`. It accepts no DSN argument, reads only the explicitly injected `CONTROL_DATABASE_URL`, creates a short-lived independent control engine, delegates to the same redacted platform-admin CLI parser, and disposes the engine afterward. Missing configuration and unexpected runtime failures emit one fixed message; launcher tests prove a supplied DSN argument and its embedded password are never echoed. The deployment-specific environment injection and controlled production smoke remain operational work rather than hard-coded repository credentials.
- Platform tenant discovery now starts with a control-database-only minimized directory. Authentication and `platform.tenants.read` authorization occur before query or target parsing; list input permits one bounded page/page-size/status tuple and detail accepts one canonical tenant UUID. Responses contain only public tenant identity, state/version, subscription expiry and non-secret route readiness, never tenant settings, database names, accounts, phone data or business rows. Each successful list/detail appends a non-PII platform audit before the DTO is returned.
- Platform tenant-business diagnosis is independently routed and one tenant at a time. Rental, device and warehouse lists now share one operation-driven authorization, target/query validation, SELECT-only routing, read-only tenant transaction and audit-before-return implementation rather than copying a route-specific runtime. The masked rental SELECT omits address, notes, buyer and waybill columns; the device projection omits serial numbers and free-text lifecycle reasons; the warehouse projection omits contacts, phone, address, printer and provider-binding data. All are bounded to 100 rows per page with a deployment-owned query timeout. A separate exact-rental PII detail additionally requires `customer.pii.read` plus a per-request reason code, returns only one main rental's customer name/phone/structured address, and records the resource, reason and `pii_revealed=true` before returning. Missing resources, rejected input and failed reads audit `pii_revealed=false`; a success-audit failure suppresses the already-read result. The desktop keeps full PII and reason only in component memory, loads the first device/warehouse pages through one fenced overview, and clears/cancels every business DTO on dialog/drawer close, tenant switch or unmount. Cross-tenant search, aggregation and export remain unavailable.
- Default migration can now close the historical-snapshot step for a genuinely empty source without deciding the unresolved non-empty credential lineage. The verifier binds the manifest to the tenant schema's unique `database_identity` and exact schema generation, then requires zero legacy outbound/inbound tracking rows, zero shipped/returned/completed or actual-shipment facts, zero print-like legacy audit facts, and zero Core shipment/provider-attempt/print ledgers. Only that all-zero tuple produces a manifest/source-snapshot-bound stable phase digest; any hint returns the fixed `HISTORICAL_SNAPSHOT_REQUIRES_APPROVED_NONEMPTY_ADAPTER`. It performs no writes and cannot create or select a credential revision, shipment, print job, provider call or physical print. Non-empty history remains blocked on the D61 credential-ownership decision.
- D64 simplification is reflected in the backfill executor: after resolved mutation steps and the approved historical adapter run, it executes the single existing 12-key policy/collector runner and advances the existing migration journal only when that ordinary check passes. The separate reconciliation receipt file/store, standalone snapshot-receipt command and their duplicate persistence tests were removed. A blocked/unknown finding still leaves the journal at `backfill_verify`, while there is no candidate signature, receipt artifact or additional hard release gate.
- Default-migration application enforcement now has a concrete Session adapter. Its machine evidence is bound to the manifest, implementation identity and migration bundle and contains separate trusted-route, platform/tenant namespace, effective-gate and legacy-surface-negative test digests with an enforced zero count for production write identities, provider calls and prints. In one final control transaction it current-locks the tenant, route and database-identity record, requires exact database/schema UUID, revision, generation and digest agreement plus separate DML/platform-read identities and active login convergence, writes or replays the fixed D60 grant, then publishes only the exact `provisioning/provisional` pair as `active/ready`. Drift rolls back grant and publication together; replay does not extend the grant or bump row versions.
- Database/jobs and contract now have separate manifest/implementation/bundle-bound machine-verifier adapters. Database/jobs requires six independent observations for grants, fleet schema, scheduler absence, single durable worker, outbox/provider fences and cross-schema denial. Contract requires an observation-window identity, legacy schema, route/config/bundle and recovery negative scans, provider-snapshot preservation, and literal zero counts for D61 legacy authorities, global writers, production-write test identities, provider calls and prints. Neither adapter discovers a DSN or performs the actions it verifies. An exact five-phase Mapping registry rejects any missing or phase-swapped executor and requires the specialized reconciled backfill executor, so the existing CLI cannot compose a purported complete chain around a generic backfill digest. The registry has not been run end-to-end because non-empty D61 history and real-MySQL expand work must still fail closed first.
- Expand now has explicit control and tenant infrastructure verifier steps ahead of in-place registration. Control evidence binds the exact control head, migration round trip, metadata/model match, least-privilege account grants and installation marker. Tenant evidence binds the exact tenant head, migration round trip, metadata/model match, immutable database-identity observation, distinct tenant-DML/platform-read grants and cross-schema denial. Both bind the manifest, implementation and migration bundle and enforce zero production-write test identity/provider/print use. Explicit-connection Alembic and fixed-query account/grant observers are implemented, but the first isolated-MySQL end-to-end run remains pending; these adapters do not infer a DSN or claim that fake/SQLite evidence proves MySQL compatibility.
- The default-migration CLI now composes an exact complete registry without silently crossing the irreversible authority boundary. `run-to-authoritative-boundary` resumes from the journal and applies only expand, backfill/verify, application enforce and database/jobs enforce; it always stops before contract. Recording the UTC tenant-aware-write marker is a distinct one-way CAS, and `run-contract` rejects an absent marker or unfinished prerequisite. Missing/extra phase executors are rejected before mutation, while complete-journal replay executes no steps. This orchestration accepts only injected executors and neither discovers a DSN nor enables provider/print side effects. Offline manifest/serialization/initialization failures emit only `MIGRATION_DOCUMENT_REJECTED`; execution-class failures emit only `MIGRATION_EXECUTION_REJECTED`. A deliberately credential-like unexpected executor exception produced no traceback/message and left the journal byte-identical.
- Expand Alembic execution now has an explicit-connection non-production runner and a typed evidence binding. It accepts no DSN/environment/Flask database and rejects any caller transaction. Before DDL it allows only a file-backed SQLite database under an explicit scratch root, or an explicitly authorized MySQL 8.0.30+ connection whose current database is exactly `inventory_management_test`; no MySQL path was enabled in this validation. The script directory must have exactly the configured head. Qualification and apply use distinct factories: the scratch database performs upgrade/downgrade/upgrade, while the apply database only moves from baseline/current head to head and is never downgraded. Their anonymous target identities (canonical SQLite file or MySQL server UUID plus schema) must differ, preventing two factories from accidentally pointing the downgrade-capable qualification at the apply database; a qualification-head mismatch stops before the apply connection is opened. Both must have zero ORM metadata differences before their receipts can be combined with account/marker or tenant-identity/grant/cross-schema observers. Two temporary control SQLite files completed this flow at the then-current head `202608220028`; after provider-account schema addition the same tests were advanced to current unique head `202608220029`. The earlier synthetic `202608220024` fixture remains correctly rejected as stale before DDL.
- Purpose-separated MySQL accounts now have fixed-query grant and cross-schema observers plus a tenant dual-account matrix bound directly to tenant expand evidence. Control-app and tenant-DML profiles accept only exact schema-level SELECT/INSERT/UPDATE/DELETE; platform-read accepts only SELECT/SHOW VIEW. Global, table, column or routine grants, GRANT OPTION, missing/extra privileges, active roles and even inactive applicable roles all reject the receipt. The matrix requires distinct DML/platform-read usernames and factories. Each account first proves a bounded read of the selected schema's `alembic_version`, then issues only a token-validated foreign-schema `SELECT ... LIMIT 0`; only MySQL permission errors 1044/1142 count as denial, while success, missing-table and transport errors fail closed. These observers accept bound account connections rather than DSNs/passwords. This run used fake connections only, so no actual MySQL grant claim is made.
- Tenant expand no longer requires an impossible pre-registration identity observation. After forward-only DDL, the qualified verifier uses the existing idempotent registration service in its own Session transaction to create or exact-replay the singleton `database_identity`, binds its immutable manifest/tenant/database/generation/created-at facts, and only then runs the dual-account grant matrix. The later in-place registration step safely replays that identity before writing control registration. A concrete control-installation observer also requires exactly one matching non-retired installation with canonical UUID, positive row version and stable creation time; missing, duplicated-live or mismatched markers cannot produce evidence. The marker observer does not create the host/control identity, so controlled installation bootstrap remains an operator/composition prerequisite.
- The tenant Alembic environment now honors a caller-bound Connection and explicit metadata before any Flask application lookup, so isolated tooling never reconstructs a credential-bearing URL. Four warehouse foreign keys in Device, InspectionRecord and Rental now carry the exact names already created/dropped by the SaaS migrations; without those names, a metadata-created head could not execute the declared downgrade. An automated two-file SQLite test creates current tenant metadata, stamps the unique `20260823_xianyu_sync_state` head, derives a `20260807_damage_notes` structural baseline through the segment's own downgrades, then completes scratch baseline→head→baseline→head and apply baseline→head with zero metadata differences and named-FK checks. This is a real SaaS-segment proof, not a claim that the oldest historical migration chain bootstraps an empty database: the repository's initial revision assumes pre-Alembic business tables such as `devices` already exist.
- A complete expand composition now runs as one automated integration slice over four distinct temporary SQLite databases. It performs the real control empty-schema qualification/head apply, installation-marker observation, tenant SaaS-segment qualification/forward apply, singleton tenant database-identity creation/replay, first Admin/user/membership and control identity registration, provisional route persistence and journal CAS. The completed journal advances only to backfill/verify; control/tenant rows remain unique and `provisioning/provisional`, and an exact phase replay returns the original evidence without invoking the executor again. Only the MySQL grant/cross-schema matrix remains deterministic in this test, so it is not a MySQL qualification claim.
- The irreversible writer marker now has an ordered time invariant in addition to phase ordering. Its timestamp cannot predate database/jobs-enforce completion, and a first write cannot be later than the runner's current UTC; future/backdated or different-time replays reject while exact persisted replay remains stable. Migration manifest fixtures were aligned to the actual current unique control and tenant heads instead of stale synthetic values.
- Registration publication now requires a trusted UUID-to-engine resolver and a dedicated independent lock connection; no DSN, environment, scoped-session, or control-database fallback is permitted.
- Tenant and control Alembic environments preserve host-process loggers during in-process migration runs, preventing migration validation from contaminating later application tests.

## Validation results

All commands ran locally with no real database/provider gates enabled.

- Unit suite: `1913 passed` in 41.71 seconds.
- Integration suite: `146 passed, 5 skipped` in 22.47 seconds. The skips are gated real-MySQL tests.
- Repository-root backend suite after the platform redemption reveal-limit, platform device/warehouse reads and tenant invitation handoff: `2479 passed, 5 skipped` in 130.07 seconds. The skips are gated real-MySQL tests; no production or `inventory_management_test` connection was enabled.
- Complete desktop frontend suite after the redemption-code metadata/API, reusable status index, platform device/warehouse overview and invitation flow: `50` files and `378` tests passed in 21.79 seconds; `vue-tsc --build` also passed. The mobile app passed `vue-tsc --noEmit` and a production Vite build; generated build output was removed after validation so source-only worktree scope was preserved. No real provider, print path, MySQL socket or browser persistence was used, and the performance threshold remains unchanged.
- Tenant invitation HTTP coverage proves authenticated Admin CSRF/RBAC, actor-phone D48 for Admin invites, no D48 for Operator invites, one-time link rotation/revocation, fragment credential inspection, bound SMS acceptance, atomic membership creation and wrong-code attempt persistence without target user/invitation leakage. Existing invitation persistence/state tests prove exact replay, loser supersede and first-membership-wins; real MySQL contention and production proxy/APM redaction remain release-matrix work rather than unit-test evidence.
- Core plan revision/snapshot closure is backed by the `plans(code, revision)` identity, composite snapshot foreign keys, frozen plan/code/subscription digests and the v1 canonical entitlement parser. Parser tests reject every candidate device/rental/integration quota and any seat value other than 10; invitation persistence and seat arithmetic count only active memberships plus database-time-unexpired pending invitations, allowing the tenth seat and rejecting the eleventh.
- Job recovery policy now has a versioned immutable registry covering provisioning, SMS, SF, physical printing, Xianyu/Kuaimai synchronization, backup and system cleanup. Ambiguous provisioning/SF outcomes are query-and-reconcile only; SMS/print require explicit confirmation; sync/backup/cleanup allow automatic retry only with an immutable snapshot, stable idempotency and the original fence. Unknown categories fail closed, unsafe policy combinations are rejected, and the focused recovery-policy plus durable-job/outbox suite passed `52` tests.
- Tenant membership mutation is split into typed contracts, one reusable lock/recount component and a bounded transaction service rather than extending the invitation runtime into another monolith. Operator enable/disable/release is exposed through the same CSRF/RBAC control-only member surface on desktop and mobile; disable/release revokes target sessions and increments auth authority atomically. Admin permission changes require an exact internal D48 proof and the ordinary HTTP route currently fails closed until task 4.5 supplies a consumed action intent. The tenant coordination lock plus current active-user/Admin recount prevents removing the last effective Admin. Focused membership/RBAC/invitation HTTP coverage passed `23` tests; desktop API coverage passed `6` tests and both desktop/mobile type checks passed.
- Tenant identity/session/login and eight-extension atomic composition focused suites pass; the latest subscription/identity/access/composition backend run is `54 passed`, while the login/session-event plus control-migration run is `19 passed`. Control session-anchor and auth-security-event upgrade/downgrade plus metadata checks pass.
- SMS official-adapter/trusted-proxy/challenge/login/composition focused suite: `59 passed`.
- Subscription projection evaluator/durable scheduler/worker plus shared job-runtime focused suite: `39 passed`; evaluator heartbeat/operational signal/health focused run: `35 passed`.
- Operational freshness, current-signal, health, subscription-projection and job-heartbeat focused run: `50 passed`.
- Queue aggregate plus operational freshness/current-signal/health focused run: `41 passed`.
- Backup domain, persistence, publication, NAS pull and NAS retention focused run: `98 passed`.
- Platform tenant-read query/runtime/route focused run: `40 passed`; it covers SELECT-only account routing, masked lists, exact full-PII projection, bounded selectors, unreadable tenant states, malformed/duplicate queries, separate PII capability, reason/resource audit, audit-before-return and runtime fail-closed behavior without a production/test database connection.
- Empty-history snapshot verifier, resolved phase adapters and default reconciliation registry focused run: `13 passed`; all databases were ephemeral SQLite fixtures and no provider/print adapter was invoked.
- Reconciliation domain/collector/journal focused runs pass; the backfill phase advances only after the complete ordinary reconciliation report passes.
- Application-enforce, D60 grant and phase-composition focused runs pass; all control data used ephemeral SQLite and no tenant/provider/print endpoint was contacted.
- Database/jobs, contract, exact executor registry, application-enforce and phase composition focused run: `26 passed`; it includes authoritative-marker enforcement and rejects non-zero legacy authority or side-effect evidence.
- Expand infrastructure, database/jobs, contract, exact registry, application publication and backfill reconciliation focused run: `31 passed`; all verifier results were deterministic in-process fixtures and did not connect to a real database.
- Complete migration-command orchestration, CLI, journal runner, phase executor and exact-registry focused run: `33 passed`; it covers the four-phase pre-authority chain, mandatory boundary stop, explicit marker, separately authorized contract and exact replay without a database or provider connection.
- Explicit-connection Alembic qualification/apply, control expand evidence binding, current-head checks and existing control/tenant migration smoke focused run: `16 passed`; real upgrade/downgrade/upgrade and forward-only apply ran only on temporary SQLite files, and the MySQL/provider gates remained disabled.
- Expand qualification/apply, singleton tenant-identity establishment, control-installation observation, fixed-query MySQL grant/dual-account denial matrix, physical-target separation and in-place registration focused run: `36 passed`; all MySQL results were deterministic fakes, real writes/round trips used temporary SQLite files, and no socket was opened.
- Tenant SaaS-segment round-trip/forward-apply, current-head metadata, named warehouse-FK and impacted rental/inspection/model focused run: `61 passed`; all database writes used temporary SQLite files.
- Complete four-database expand composition: `1 passed` in 4.13 seconds; all DDL/DML used temporary SQLite, while the account grant matrix remained a fake and no socket opened.
- Redacted migration CLI, command and journal-runner focused run: `22 passed` in 1.13 seconds; malformed documents retain the document error class, while missing/failing execution adapters return only the fixed execution error class and leave the journal unchanged.
- D64-simplified migration policy/registry, schema observer, phase executor, adapters, contract registry and command focused run: `30 passed` in 2.53 seconds; no receipt artifact was created, SQL writes used temporary SQLite only, and no socket opened.
- Default migration current-head, phase/journal/command, schema/grant/expand/backfill and migration smoke regression after fixture alignment: `183 passed`.
- Invitation expiry runtime plus invitation persistence/state-machine focused run: `44 passed`.
- Platform host create/reset, setup-only flow, login/rate-limit/audit, independent Cookie/CSRF HTTP boundary, session self-management, route runtime, atomic composition and platform-identity migration focused run: `36 passed` (one existing SQLAlchemy warning).
- Desktop platform setup/login/session-management API, views and platform-namespace guard focused run: `12 passed`; desktop TypeScript check passes.
- Desktop platform tenant directory/read API and view focused run: `10 passed`; TypeScript check passes, including masked-list tenant-switch fencing and memory-only full-PII clearing.
- Desktop identity/login/status/account-security/subscription focused tests include a latest `9 passed` API/status run; desktop and mobile type checks pass.
- D33 focused suite: `113 passed, 2 skipped` in 4.43 seconds.
- D17/D33 shared-policy regression after moving the legacy find-slot compatibility path off its per-device logistics-window check: `73 passed`; inclusive usage overlap remains blocking, a two-day logistics overlap remains a non-blocking warning/relay candidate, and the same policy is exercised by booking, final rental mutation, normalized Gantt and relay discovery.
- Tenant accessory/logistics/shipment schema-model closure: `14 passed`; focused tests cover logical-unit/request/link/event constraints, structured planned-versus-actual logistics facts, customer/internal-note separation, exact historical integration/account credential snapshots, provider-attempt/print uniqueness and the ordered tenant migration head. Writes used the isolated SQLite test database and the MySQL check only rendered offline DDL.
- Python bytecode compilation: passed for `app`, `inventory_control`, `control_migrations`, `migrations`, and `tests`.
- Desktop frontend full unit suite now has `388 passed` functional assertions across 52 passing files. `vue-tsc --build` passes; no threshold was changed.
- Desktop production build: passed (`2759` modules), including the platform tenant masked/full-PII views; the existing large-chunk warning remains.
- Mobile production build: passed (`424` modules), including the default-warehouse setup route, account-security route and D34 internal-note control.
- Capability-specific job recovery policy plus job/outbox focused run: `52 passed`; provider provisioning/SF ambiguity requires query reconciliation, SMS/physical print requires explicit confirmation, and safe retry categories require immutable snapshots plus stable idempotency/fencing.
- Tenant member mutation, invitation and RBAC focused run: `23 passed`; Operator enable/disable/release, realtime seat recount, target session revocation and last-effective-Admin protection are implemented, while Admin authority changes remain fail closed until the D48 intent confirmation path is connected. The implementation is split into contracts, locking and service modules rather than extending the HTTP runtime into a monolith.
- D48 persistence foundation uses migration `202608220027` for two payload-redacting intent/challenge-link tables and migration `202608220028` for challenge/intent/action-outcome security-event references; both segments complete upgrade/downgrade and current-head ORM metadata comparison. Shared canonical action payload code is used by both SMS and sensitive-action protocols; the root-key-derived context MAC binds tenant, actor, session, subtype, target, revision, canonical request digest and idempotency key. Focused migration/crypto/SMS coverage includes a fail-closed fix that prevents malformed protocol inputs from comparing equal to an all-zero MAC.
- The shared D48 intent service now also prepares and replays one fixed-role `old_phone`/`new_phone` pair without duplicate delivery. Dual verification runs inside a savepoint: if the second code is wrong after the first code matched, both tentative state changes roll back and only the failing challenge receives one committed wrong-attempt increment; both challenges are consumed only when both codes match, leaving the caller free to commit or roll back the protected phone mutation in the same outer transaction. The focused sensitive-action suite covers pair persistence, exact replay, role swapping, partial failure, successful atomic consumption and completion; the following vertical slice now consumes this protocol.
- The phone-change vertical slice now consumes that dual-code authorization through one D47-coordinated final transaction. It creates a temporary new-phone coordinator only inside the rollback-protected savepoint, locks canonical users, every pending invitation and affected tenant/seat guard before intent/challenge/session rows, and accepts only an invitation-created unverified coordinator with no membership, session, registration, sensitive-action or security-event history. It then supersedes all cross-tenant pending invitations, invalidates/detaches acceptance challenges, deletes the empty coordinator, preserves the current immutable user UUID, updates the canonical E.164 metadata and verification timestamp, increments auth version and revokes every old session. Wrong second-code coverage proves the correct old code and temporary coordinator remain unconsumed/uncommitted. The HTTP route and desktop/mobile account-security clients keep one action UUID in component memory, clear tenant CSRF after success and require login with the new phone. The combined D48/member/invitation/identity focused backend run is `63 passed`; desktop phone-change/API focused coverage is included in `13 passed`, and both frontend type checks pass.
- Member mutations now use one shared lock coordinator matching the confirmed D47/D48 order: related users, their pending invitations, tenant/seat guard, memberships, then the optional sensitive intent, open challenges and target sessions. Challenge issue takes the same domain locks but lets the SMS service acquire its own rate-limit-subject/challenge order. This removes the earlier issue-path `intent → challenge` versus confirm-path `challenge → intent` inversion, while preserving exact completed-action response replay after the target membership revision advances.
- Admin invitations now use the same persistent D48 intent service rather than the older direct challenge submission. The browser preallocates one action UUID; the context binds actor user/session, tenant/access version, expected-absent invitation UUID, Admin role and canonical target phone. Invitation persistence invokes an internal authorizer only after its D47 ownership/seat locks are held. A wrong code rolls back the nested invitation/user work, then commits exactly one failed attempt and one rejection event; success consumes the challenge, creates the invitation, marks the intent succeeded and appends verified/committed events in the same outer transaction. The action UUID is also the invitation UUID, and a domain-separated/versioned root-key PRF derives its 256-bit URL-safe token so exact response-loss replay returns the same still-valid fragment while the database retains only the token hash. Replay after token rotation, revocation or expiry fails rather than reviving the original link. Focused backend coverage is `57 passed`; desktop API coverage is `8 passed`, and desktop/mobile type checks pass.
- D57 is executable as a negative authority boundary rather than an undocumented omission: the platform role has no tenant phone/member/delete authority; the registered tenant-scoped platform mutation surface contains only the already approved subscription-adjustment endpoints; and the host parser rejects tenant phone change, impersonation, OTP submission, phone override and last-Admin recovery commands before any tenant user can be written. The RBAC, route-surface and CLI focused run is `34 passed`.
- The first complete D48 action slice now covers Admin membership enable/disable/release: issue and confirmation share one client-generated action UUID, only the actor's canonical phone receives the primary challenge, wrong attempts commit without member mutation, exact context consumption and member mutation finish in one control transaction, last-Admin protection remains active, and response-loss replay returns the persisted safe member result without reusing the OTP. Desktop and mobile clients are connected; Operator mutation remains on the no-D48 path. The focused intent/member/SMS/migration runtime run is `49 passed`; both frontend type checks pass and the desktop tenant-identity API run is `7 passed`.
- Tenant integration credentials now have a control-only D48 vertical slice. Safe metadata create/list never projects a credential or current revision UUID; challenge issue binds the provider, server-derived current pointer, integration row version, tenant access version and canonical credential-semantics digest to the active Admin actor's phone. Confirmation creates an immutable encrypted `pending_validation` revision, consumes the exact challenge and enqueues one stable provider-validation outbox in the same control transaction. Wrong-code coverage proves the nested revision/outbox work rolls back while the attempt persists; response-loss replay proves revision and outbox identities remain stable. D56 rejects expired access before credential parsing or SMS/provider I/O, and all provider behavior in this `36 passed` focused run used an in-process fake with SQLite only.
- Desktop and mobile tenant integration settings now consume only the safe metadata/create/challenge/confirm contract. Both clients support the exact SF, Xianyu and Kuaimai credential schemas, preallocate stable create/action UUIDs and keep credential values, OTP and challenge state only in component memory; field/connection changes invalidate the proof, an ambiguous network result retains exact retry state, and success/unmount clears it. The desktop API/view focused run is `5 passed`; desktop and mobile type checks pass, and the mobile production build completed with `434` transformed modules before generated output was removed from the worktree.
- Ordinary control-outbox dispatch now has a reusable transaction-owning runtime rather than provider-specific loops. It commits claim and exact side-effect authorization before external execution, authenticates only allowlisted safe-result digests, and persists the outbox terminal state together with the handler's business projection; preparation failures remain before the side-effect boundary, while adapter exceptions, malformed results, failed final persistence and explicit unknown outcomes quarantine without automatic resubmission. The integration handler binds one outbox event/generation to one encrypted credential revision and deterministic attempt UUID, decrypts only the submitting revision into a one-shot redacted adapter request, and maps valid/invalid/unknown results through the existing integration CAS service. Current D56 authority checks tenant recovery/suspension/subscription/route/access-version and source generation before claim, dispatch and final result; suspended coverage proves the adapter is not called. The combined outbox/integration focused run is `44 passed`; only fake adapters and SQLite were used.
- Existing SF account-claim foundation was revalidated against the new integration worker boundary: canonical keyed fingerprint, domain/persistence reservation, activation, Admin release, deletion release, rebind, loser non-disclosure, outer rollback and isolated migration coverage pass `36` focused tests. This is still a foundation rather than an exposed binding workflow: provider verification, warehouse binding, transfer reconciliation and D48 HTTP composition remain open.
- SF provider-account persistence now extends that claim foundation without exposing a provider or production database. Control migration `202608220029` creates stable account identities, isolated encrypted account-secret revisions and envelope-event history; MySQL offline DDL, SQLite upgrade/downgrade and full control metadata comparison pass. The caller-transaction service binds each pending account revision to the exact API-connection revision and claim fences, activates claim/account current state atomically for a known provider success, keeps failure/unknown from switching current, supports exact historical decryption, and releases D50 claim/account state atomically. A separate tenant-session service applies monotonic warehouse bind/replace/unbind facts, and the control resolver rejects any tenant/warehouse/binding/account/claim/fingerprint/integration mismatch while allowing shipment-frozen historical revision resolution after unbind. The combined credential/claim/account/binding/migration focused run is `70 passed`; all writes used ephemeral SQLite, all provider behavior was absent, and no network socket or print path was used. D48 HTTP/outbox, a real SF validator, cross-fingerprint/tenant transfer saga, tenant/control reconciliation and shipment composition remain open.
- Backend full suite after control head `202608220029`, tenant head `20260823_xianyu_sync_state`, provider-account encrypted revisions/envelope rotation, D48 bind/unbind HTTP, claim/account transaction coordination, tenant warehouse binding apply/removal, shipping/tracking/printing ledgers, tenant-bound Xianyu scheduling/credential/provider/worker composition, the shared durable process host/capability composer, routed tenant transactions, server-derived SF order identity, exact-revision SF credential requests, shared retry backoff, priority promotion, exact job replay validation and legacy provider isolation: `2783 passed, 5 skipped` in `185.20s`; all provider clients were fake and no real database, printer or production service was configured.
- Desktop frontend full unit suite after the shipment-ID tracking/cursor client and Xianyu local-summary/202-job migration: `53` files and `394` tests passed; the complete TypeScript project check also passed.
- Focused provider-account HTTP/validation/binding/unbinding slice: `63 passed` across encrypted account revisions, control migration parity, claim+revision coordination, D48 bind/unbind issue/confirm and exact replay, wrong-code rollback, exact API/account validation, current D48/D56/source authority, unknown-result quarantine, monotonic tenant binding planning, provider-success → tenant-binding apply, and claim-release → tenant-binding removal. The unbinding worker remains authorized by the immutable old release event after a different tenant re-reserves the fingerprint, while an old HTTP replay cannot release that newer ownership. The tests used only in-memory/local SQLite and injected fake validators/appliers; no real provider, printer, production tenant route, or production database was contacted.
- Expanded provider-account credential/claim/service/worker/HTTP focused run: `79 passed`. The Admin account-settings projection returns only safe account/integration/warehouse/binding/status/masked/version facts and reconstructs inactive-account warehouse ownership from the immutable release event without exposing claim/revision UUIDs, fingerprint, ciphertext, digest or plaintext. Unknown validation now supports an explicit non-submitting reconciliation primitive: `still_unknown` remains quarantined and exactly replayable, while confirmed success/failure reuse the ordinary validation state transition and claim/account/current-pointer fences; a second terminal reconciliation is rejected. No real provider, database route or printer was used.
- Focused account-envelope rotation: `15 passed` across provider-account service and control migration tests, including exact revision rewrap, append-only envelope event, idempotent replay, unchanged plaintext semantics under the new root, and rejection by the retired root. This is an in-process crypto/database test only and does not claim production root rollout.
- Warehouse-move relay-chain/shortage-boundary focused run: `107 passed` across the warehouse service, shared relay-chain solver, warehouse HTTP runtime, Gantt reorder regression, and shipment/waybill/print execution ledger. It covers target-warehouse reassignment followed by requestless neutral-successor propagation, deterministic shortage with old ordinary/neutral links removed, stable preview fencing, rollback on unsupported cross-device relay facts, request/link/unit/current-warehouse rechecks immediately before waybill/print effects, and no logical-unit UUID in public results. No provider, printer, or non-SQLite database was used.
- Shipping/warehouse execution focused run after server-side printer routing: `77 passed`. The reusable caller-transaction binding service applies only already provider-verified metadata, exactly replays an immutable command, supports same-warehouse replacement, rejects one SN across two warehouses and resolves only ready-warehouse active+verified Kuaimai rows. Paired-print preparation no longer accepts a caller-selected printer; it reuses that resolver after locking the shipment/device/warehouse/accessory facts and persists the same SN on both immutable label jobs. Missing/inactive/verification-failed bindings create no job, and a binding change after preparation blocks provider submission without rewriting the historical pair; exact preparation replay still returns the original SN snapshot. Tests used model-only fake printer rows and made no physical or provider print call.
- New-shipment identity is now server-derived from the typed tenant context and a stable shipment UUID. `ShippingExecutionService.prepare_shipment` no longer accepts a provider-order string; it uses the shipment UUID as the ledger primary key and derives the only SF identity as `sf:<tenant_uuid>:<shipment_uuid>`. Replay locks that exact UUID and compares the full immutable snapshot, while customer name, phone and address cannot enter the provider identity. The combined routed-transaction/Xianyu/shipping-ledger/historical-tracking focused run is `83 passed`; it used SQLite and fake/absent provider clients only. Control-job publication, real SF create/query adapters and response-loss reconciliation remain open.
- Provider-context/warehouse/shipping composition focused run: `97 passed`. New shipment preparation accepts one typed control-resolved SF context, then locks the tenant rental, selected main device, actual ready warehouse, active local account binding and accessory fulfillment before committing the ledger. Origin IDs, structured sender snapshot and tracking phone last four are server-derived; the method no longer accepts caller-supplied versions. A mismatched local account/revision, moved device, unfulfilled accessory or historical context for a new provider order creates no shipment, while an exact existing provider-order replay retains its original warehouse and credential snapshot. No provider, printer or non-SQLite database was contacted.
- SF historical tracking and create-waybill credential preparation now share one exact-revision loader instead of duplicating root-key and nested one-shot decryption logic. It verifies tenant/integration/account/claim-generation/binding/masked facts, selects only each revision's recorded root-key version, and exposes credentials to one immediate consumer. The waybill request keeps deep-copied shipment snapshots and a derived provider identity while redacting PII/credentials from representation; a binding-fence mismatch fails before an adapter. The provider-account credential suite is `19 passed`. A full SF payload still requires the scheduled-time/cargo snapshot and an injected provider adapter, so no SF endpoint was called.
- D40 ledger regression now covers the complete local cancellation/rebuild fence: a submitted old-warehouse shipment blocks a new provider order after the device moves; an unknown cancellation remains `cancel_unknown` and still blocks; explicit reconciliation to confirmed cancellation is the only transition that permits a replacement ledger using the new warehouse, account binding, sender snapshot and provider order. Printer submission against the old warehouse remains blocked, while the old shipment/attempt snapshots are unchanged. This was model/service-only testing with no SF call.
- Historical shipment tracking now has a provider-free query/planning boundary. The tenant list uses indexed `(submitted_at, shipment UUID)` keyset pagination with a 1–100 bound and a fixed non-PII summary rather than the legacy table-wide list. Selected shipments are validated and grouped by their exact integration/account/revision, original warehouse binding and frozen phone last four, then split at SF's 100-waybill batch bound. The control-side factory accepts only a typed historical context that matches every frozen technical reference, selects each revision's exact active/legacy root-key version, authenticates both encrypted revisions and emits a single-consumption redacted request; an available newer key is never a fallback. A typed dispatcher accepts only bounded per-shipment route events, fills explicit not-found results and discards credential references on success, malformed response or adapter exception. The `/api/sf-tracking` list/single/batch routes now require an explicit trusted runtime and shipment UUID contract; absent composition returns private no-store 503 even when legacy SF environment values exist. A production-shaped SDK adapter requires explicit endpoint mode and provider timezone, consumes exact request credentials once, classifies a non-success envelope as unavailable and emits only typed events; its tests use a fake client. An optional typed adapter now installs the tracking runtime in the same atomic SaaS graph and the bundle rejects any control DB, tenant boundary or routed-session split; the composition/route run is `16 passed`. The desktop view now uses server cursors and shipment IDs, displays the non-PII summary and refreshes only the current page batch; its `3` focused tests and full TypeScript check pass. No SF endpoint, printer or real database was contacted.
- The tenant-bound Xianyu sync slice now has one reusable 180-second scheduling contract rather than another Web timer. The control scheduler deterministically staggers active tenants, freezes every active Xianyu integration/current-secret revision plus row versions, and incorporates a canonical connection-set digest into the time-bucket idempotency key. Manual refresh locks the current tenant, re-evaluates an injected current-authority gate, reuses any scheduled/manual in-flight job or matching current-bucket terminal job, and otherwise creates a high-priority `xianyu_alert_sync_now` with an exact revision-only payload. Tenant migration `20260823_xianyu_sync_state` adds aggregate/per-connection status, a private provider cursor and exact alert ownership. Its caller-transaction applier is job-idempotent, advances a cursor and replaces alerts only for a successful connection, preserves failed/limited connection state, clears an old cursor on credential revision change, rejects incomplete result sets and revision drift, and increments the aggregate revision only from persisted results. The control one-shot factory locks the exact current integration/revision and both job-frozen row versions, selects only the root-key version named by that revision and exposes `app_key/app_secret` through one redacted single-consumption request. The typed provider boundary requires explicit bounded HTTPS endpoint/connect/read/rate-limit/paging settings and maps malformed responses, failures and rate limits to safe results. A production-shaped adapter uses the existing compact-body signature and status-12 page-number contract, requires a complete consistent count, applies the confirmed strict `pay_amount > 5000` rule, rejects duplicates and projects only typed PII facts. The known API does not publish an opaque incremental cursor, so the adapter neither invents nor submits one. A reusable durable provider-call authorizer current-reads lifecycle authority and renews the lease before each request in a multi-call job; the Xianyu handler uses it per connection, isolates credential drift/failure, and applies the complete result set through a final short tenant transaction. Its immutable recovery category is wired into the existing versioned policy registry: an exception or expired `provider_submitting` lease can auto-retry only for the confirmed sync safe-retry policy, with the same job payload/idempotency identity, a live deadline, remaining attempt and a fresh current-authority check; unregistered provider work still goes to review. The trusted HTTP runtime reads the local summary, closes the tenant session before control enqueue, returns `202 + job_id/snapshot_revision`, and fails closed without explicit composition even when legacy Xianyu environment values exist. Desktop polling is local-only, visible-page-only and fixed at 180 seconds; the UI consumes job receipts and shows syncing or more-than-two-cycle stale state. The combined worker/credential/provider/HTTP-adapter/persistence/store/scheduler/migration regression is `60 passed`; focused desktop coverage is `11 passed`, and TypeScript passes. Every HTTP test used an injected fake client; no Xianyu endpoint, production database or external service was contacted. Production worker launcher/composition, readiness qualification, measured production parameter values and any future provider-supported cursor contract remain open.
- Follow-up URL-policy and safe-retry/reclaim coverage raises that combined Xianyu/durable-worker regression to `64 passed`; repository-wide strict OpenSpec remains `22 passed, 0 failed` and `git diff --check` remains clean.
- Xianyu durable-worker composition is now explicit and reusable: one shared bounded tenant router feeds short routed tenant transactions; scheduled and manual job types share the same exact-revision handler, per-call authority, lease and clock; the dedicated claim scope includes only those registered types, so a higher-priority unrelated job remains untouched rather than being claimed and reviewed by this worker. Provider/configuration discovery is absent. Constructor coverage proves the graph is built without a control/tenant connection or provider call, rejects partial settings, and defaults only to the production-shaped HTTP adapter when no typed adapter is injected. The independent launcher/run loop, measured production settings and real readiness remain deliberately outside this local composition. The expanded Xianyu/durable-worker/migration focused run is `104 passed`.
- A reusable process-neutral durable-job loop now composes periodic fan-out and bounded queue draining without duplicating provider-specific worker loops. Every cycle shares one aware UTC schedule snapshot, yields back to fan-out after a configurable job budget, and sleeps interruptibly only after observing an idle queue. Its Blocking APScheduler host owns one immediate coalescing cycle with `max_instances=1`; unexpected cycle failure terminates the scheduler and returns one fixed supervisor-visible error instead of being logged-and-swallowed by APScheduler. A capability composer merges disjoint handler/periodic-definition bundles into one scoped worker/process, rejects overlaps, and lets the Xianyu slice export a reusable two-job/one-schedule registration rather than another process. The outer signal adapter installs nonblocking SIGINT/SIGTERM shutdown and restores prior handlers on every exit. Focused scheduler/worker/process/Xianyu-composition plus Web no-start coverage is `66 passed`, and a real BlockingScheduler fail-fast smoke printed `host-fail-fast-ok`. Production dependency/configuration parsing, unique executable launcher and restart/readiness remain open.
- Durable retries now require one explicitly injected immutable backoff schedule shared by the composed worker rather than handler-local constants. The first and later failed attempts persist a future `available_at`, the final configured delay caps later attempts, an exact future provider deadline takes precedence, and an already expired provider deadline falls back to the shared delay instead of creating a hot loop. The APScheduler host also latches SIGINT/SIGTERM received before scheduler start. The focused job-runtime/process/subscription-projection/Xianyu-composition run passed `60` tests; it used SQLite and fake/absent providers only.
- Coalesced pending work now has one queue-level promotion primitive. It can only raise priority or move `available_at` earlier while preserving the exact payload, idempotency identity, requester and attempt budget; leased and terminal jobs are immutable to promotion. A manual Xianyu refresh that finds the scheduled job now reuses and promotes that row to priority `100`, so the server-side 180-second merge no longer accidentally leaves an explicit refresh at scheduled priority. Generic job enqueue also locks an idempotent replay and rejects drift in access version, immutable payload, requester, attempt budget or deadline instead of silently treating different work as the first operation. The expanded queue/Xianyu scheduling and shared worker focused run passed `79` tests.
- Job handlers can no longer return arbitrary outcome objects into durable persistence. `JobOutcome` now enforces a mapping-only safe result, bounded single-line reason code, aware retry deadline and boolean non-submission proof. An invalid result before a provider boundary receives the configured bounded retry; the same invalid result after a provider boundary enters review. The expanded shared queue/runtime/process/Xianyu/subscription focused run passed `82` tests.
- Normal-job lifecycle authority now locks and checks the persisted active deletion and suspension aggregates in addition to the tenant projection, recovery hold, subscription and route. A deliberately drifted `tenants.status=active` remains denied while either aggregate is unresolved; missing, malformed or failed probes remain fail-closed. The job-authority/integration-validation/provider-account-validation/runtime/process focused run passed `73` tests. External deployment-marker composition and not-yet-registered SF/print enqueue paths remain open.
- The shared durable process now supports named interval-bucket capability producers and job-type-specific current-authority delegates without creating another scheduler loop. Subscription expiry is the first system capability registered through that boundary: one cycle scans due subscriptions, records evaluator health, durably enqueues exact projection jobs, and drains them through the same scoped worker while preserving its tenant-first lifecycle locks and special provider-free authority. Same-bucket cycles do not rescan; a failed producer is not marked complete and remains restart-retryable. The projection/process/authority focused run passed `49` tests using in-memory SQLite only.
- Producer-only maintenance capabilities can now share that process without inventing dummy job handlers. The bounded invitation-expiry sweep is registered through this path and reuses the same interval-bucket/restart semantics; the invitation/projection/process focused run passed `41` tests. A separate printer-binding/shipping-ledger/model regression passed `50` tests and confirms warehouse/printer one-to-one uniqueness, exact binding replay, immutable paired-label printer snapshots, current-binding submission fences and explicit unknown-result handling without provider or printer I/O.
- The tenant constraint closure for default warehouse, one-to-one printer binding, logical-accessory reservation windows and immutable shipment/attempt/print snapshots passed `86` focused tests. Ordinary accessory allocation locks candidate units and overlapping links in deterministic order, while only an explicit agreed relay chain may share a logical unit across overlapping rental windows. Shipping transitions remain expected-status CAS operations and exact idempotent replay never rewrites historical warehouse/account/credential/binding/address/printer facts. This run used local SQLite only; real MySQL contention remains in the later migration/validation task.
- Current login/session recovery authority now locks persisted active deletion and suspension aggregates before subscription reduction rather than trusting the tenant projection alone. Drifted `active` rows therefore still reduce to suspension, cooling or deleted/committing, with deletion phases winning over an unreleased recovery hold. Job lifecycle probes use the same approved transition boundary: `pending_review` deletion requests do not freeze normal work before platform approval, while cooling and later phases do. The recovery/login/session/HTTP focused run passed `75` tests and the job/recovery/provider-authority run passed `59` tests.
- A production read-only transaction observed the legacy `inventory_management` shape without DML: 12 base tables, 139 devices, 2,094 rentals and an existing range-query plan of `type=ALL`, no selected key, approximately 2,066 examined rows plus filesort. SaaS accessory/shipment tables are not deployed there, so their new indexes cannot yet be measured against production data. The first real `inventory_management_test` contention attempt remained fail-closed before DDL because the configured account has grant authority outside the isolated test schema. A MariaDB 10.11 compatibility defect in the guard was separately fixed: a successful empty `SHOW GRANTS FOR PUBLIC` result is accepted as no explicit PUBLIC grant, while every nonempty row must still parse as no-privilege `USAGE`; guard unit coverage passed `68` tests. No production or test database write occurred.
- The legacy Xianyu compatibility module no longer discovers `XIANYU_*` process credentials or sender fallbacks: its constructor accepts explicit values only, its global compatibility instance is deliberately unconfigured, the production alert route never imports it, and `.env.example` no longer advertises Xianyu APPKey/Secret. The remaining global-session/provider-backed shipping-batch blueprint is now test-only as one whole surface; every non-test schedule/status/express/printer/print/Xianyu path returns the same private no-store 503 before handler, ORM or provider execution even if its compatibility flag is set. Focused legacy/new Xianyu and shipping isolation runs passed `37` and `42` tests respectively. This does not claim the pending exact-revision Xianyu shipment provider path or removal of SF/Kuaimai fallbacks.
- Legacy tracking/scheduler isolation now matches the shipping boundary. The registered Web blueprint imports no `scheduler_tasks` at module load, so application construction no longer initializes the environment-era global SF client. All legacy tracking query/batch/manual-scheduler/status/device-status aliases are enabled only by a literal TestingConfig flag; a non-test process returns private no-store 503 even if that flag is set. The focused tracking/security/route run is `13 passed`. Versioned Docker/production templates and Makefile checks no longer ask for generic `SECRET_KEY` or `API_KEY`; the ignored local `.env` was not changed or used as evidence.
- Provider compatibility adapters now have explicit dependency construction rather than process credential discovery. SF order, SF tracking, the old scheduler, Kuaimai print and Xianyu order modules default to unconfigured objects and accept credentials/client/sender facts only through typed or explicit constructor inputs; the application source has zero `os.getenv` reads for `SF_*`, `KUAIMAI_*` or `XIANYU_*`. Versioned environment templates, Makefile output and Docker instructions no longer advertise those provider values. The focused fail-closed/provider compatibility suite is `40 passed`, and the broader SF/relay/shipping compatibility run is `68 passed`; all provider clients were fake or deliberately absent. Existing operator-held values were not read or migrated, so creating verified encrypted default-tenant revisions remains open.
- Logical-accessory allocation closure passed `97` focused tests across the schema constraints, caller-transaction inventory service, rental final-create/update boundary, availability projection, whole relay-chain solver and Gantt HTTP projection. Ordinary requests select only active units in the main device's current ready warehouse, lock type/warehouse and then candidate units plus overlapping links in deterministic order, and persist request/link changes atomically with the rental. `total/reserved/available` is derived directly from active capacity, current holder and the requested reservation window, with no cross-request cache and no unit/link identity in public results. Relay candidates and agreed chains continue through the separate shared chain solver rather than weakening ordinary overlap rules. The run used local SQLite and no provider, printer or production database; real MySQL contention remains assigned to the later migration/validation matrix.
- The expanded accessory/relay regression passed `115` tests across ordinary inventory, whole-chain solving, tenant-session and legacy transition services, tracking, routed HTTP and batch-shipping compatibility. It includes reversed agreement order, downstream recomputation, downgrade/reallocation, out-of-order and sequential handoff, earlier-edge replay after downstream custody advancement, completion projection and audit-failure rollback. The tenant HTTP route remains fail-closed for external stages, and the provider resolver was either fake or explicitly made unreachable. This run used isolated SQLite only and made no production/test-schema write, provider request or print call; real MySQL chain contention and the complete 8.12 cross-surface matrix remain open.
- Default migration now has a version-locked source migration preflight bound as the first verified-expand step. It accepts no DSN, password or implicit application database; requires an exact MariaDB 10.11/MySQL 8.0.30+ caller-bound connection with schema-only `SELECT/SHOW VIEW`, no active role and no grant option; opens one consistent read-only snapshot; hashes schema metadata/exact per-table counts; and in that same snapshot classifies six fixed legacy/Core shipment and print categories. Unknown lifecycle columns or in-snapshot schema drift reject the preflight. Baseline or historical-boundary mismatch stops before either control or tenant expand verifier. Baseline, boundary and combined preflight have strict exact-key document serde; the offline `create-manifest-from-source-baseline|source-preflight` commands inject the source digest into templates that cannot contain a manually supplied source digest. Stable replay, version/shape/schema mismatch and ignored implicit `DATABASE_URL` are covered. The backfill builder permits the official zero-history verifier only for an empty boundary and requires a matching boundary digest plus positive approval revision for any non-empty adapter. The focused source/CLI/phase/expand/history run passed `35` tests; the expanded default-migration/control-plane regression passed `234` with one opt-in real-MariaDB case skipped after its separate successful run, and no provider/print calls.
- Target OpenSpec strict validation: passed.
- Repository-wide OpenSpec strict validation: `22 passed, 0 failed`.
- `git diff --check`: passed.
- Tracked desktop and mobile build output directories remained clean after restoration.

## 2026-08-23 MySQL 8 qualification addendum

- Two disposable MySQL 8.0.46 containers with tmpfs data and the exact schema name `inventory_management_test` were used; qualification and forward-apply targets had different server UUIDs. No production schema was written, no provider/printer was invoked, and both containers were stopped and removed after validation.
- Real MySQL contention for logical accessory allocation, Gantt reorder and warehouse movement passed `5` tests. The operation-key fixture was shortened to remain within the service's existing 48-character contract.
- The tenant SaaS migration segment passed a real baseline→head→baseline→head qualification, an independent baseline→head forward apply, ORM metadata zero-diff checks, exact DML/platform-read grants and both cross-schema denial probes: `1 passed in 8.75s`.
- The real run exposed and fixed MySQL-only compatibility defects without changing product behavior: FK-supporting indexes are no longer dropped before their constraints/tables; rental downgrade temporarily preserves an index beginning with legacy `device_id`; the singleton database-identity primary key is explicitly non-autoincrement; added-column comments match ORM metadata; and offline MySQL DDL generation avoids online reflection.
- The metadata rebuild guard now ends its read transaction before yielding while retaining its connection-scoped advisory lock, preventing a separate Alembic connection from waiting on a generation-table metadata lock. Grant observation now accounts for MySQL's intrinsic global `USAGE` and combines portable privilege inventories with an exact `SHOW GRANTS` allowlist because MySQL 8 has no `INFORMATION_SCHEMA.ROUTINE_PRIVILEGES`; extra routine/role/table/column/global authority remains rejected. Guard/grant coverage passed `83` tests.
- Post-fix qualification/migration regression passed `93` tests with the opt-in real-MySQL case skipped after container cleanup. SQLite/fakes remain useful for fast domain regression only; they are not cited as database-compatibility or migration-completion evidence.
- Future ordinary database acceptance should run against the production instance's `inventory_management_test`. The configured legacy root account remains ineligible because it has global `ALL` and `GRANT OPTION`; the guard was not weakened. On 2026-08-23 it was used only as the explicitly authorized account administrator to create three `%`-host test identities: `inventory_saas_test_migrator` has schema-only `ALL`, `inventory_saas_test_dml` has exact `SELECT/INSERT/UPDATE/DELETE`, and `inventory_saas_test_platform_read` has exact `SELECT/SHOW VIEW`, all only on `inventory_management_test` and without `GRANT OPTION`. Each identity connected to the test schema successfully, reported no active role, and received MariaDB 1044/1142 denial for a bounded production-schema read. Random passwords were stored only in the local macOS keychain; no repository or deployment file contains them.
- Four disposable MySQL 8.0.46 instances then exercised the complete expand composition with distinct control qualification/apply and tenant qualification/apply physical identities. The real path completed control base→head→base→head plus independent forward apply, tenant SaaS-segment round-trip plus forward apply, both metadata comparisons, a real installation marker, tenant identity establishment and exact replay, in-place control tenant/Admin/membership/identity/provisional-route registration, real tenant DML/platform-read grant and cross-schema-denial observation, journal completion and replay: `1 passed in 30.58s`. The control-account grant digest remained deterministic test evidence, so this run makes no claim about a real control runtime account grant. All four containers used tmpfs storage and were removed; no production schema, provider or printer was touched.
- That full MySQL path exposed and fixed additional compatibility defects: control Alembic now preserves already-rendered check names; `DATETIME(6)` defaults share one reusable dialect-aware `CURRENT_TIMESTAMP(6)` expression; manually assigned root-key versions are non-autoincrement; the invitation user FK is `RESTRICT`, consistent with its pending-status check; table downgrades no longer drop redundant FK-supporting indexes first; the auth-event batch migration uses direct MySQL ALTER behavior and bounded FK names; the sensitive-action MAC uses `BINARY(32)` like the ORM. The complete control downgrade chain now succeeds on MySQL as well as SQLite.
- A fail-closed preflight through the new schema-only migrator then observed `inventory_management_test` without changing its tables: MariaDB 10.11.6, nine existing base tables (`alembic_version`, legacy audit/device/inspection/rental/relay/statistics tables), non-empty inventory, and one drift reason, `missing_identity_generation`; the pinned digest was `27b0575a85fa38471178cecd728f3b0a43b33bf4dc67bc2bde00897d0d089050`. No business-schema DML or DDL was executed. Account creation changed only server account/grant state as explicitly requested; production business data and the test schema contents remained untouched.
- The new source observer then used only `inventory_saas_test_platform_read` against that same schema. Repeated observations reported MariaDB profile `mariadb-10.11`, 9 tables and 6,264 aggregate rows with the same source snapshot digest `86a529d2f7825c5f26f11bf36916fd4de56489d7c9103fb26c82babb38b49824`; the structural evidence digest remained `5e2392553002794a569dd2a5169a3ffcc34b14041c5efc56f4743f8b389cb56e`. The same snapshot classified 1,828 lifecycle-history rows, 1,807 tracking-history rows, zero matching print audits and zero Core shipment/attempt/print rows as `requires_approved_nonempty_adapter`; its boundary digest is `7872e738642beba9f7f2e8c9ccdc2160204af5d46d02b9c25c55774cfdf0f575`. The opt-in real-MariaDB integration case passed `1` test in `1.78s`. Every observation used `START TRANSACTION WITH CONSISTENT SNAPSHOT, READ ONLY`, selected only aggregate counts and schema metadata, rolled back and closed; no schema mutation occurred.
- The post-fix control-migration/default-expand/control-plane/database-guard regression passed `2113` tests with `2` opt-in real-MySQL cases skipped after disposable-container cleanup. The target strict OpenSpec validation and `git diff --check` both passed. One stale HTTP test was corrected to assert the existing pre-authorization behavior (`403 TENANT_CAPABILITY_DENIED`) when a suspension already exists; final in-transaction renewal-gate conflict behavior remains covered at the service boundary.

- Disposable-MySQL relay qualification added two custody-contention cases to the existing accessory module. On a fresh MySQL 8 container with tmpfs storage and an account granted only on `inventory_management_test.*`, all `4` module cases passed in `3.80s`: overlapping allocation produced exactly one winner, non-overlapping allocation produced two winners, concurrent exact replay of one A→B shipment produced one immutable handoff event, and concurrent A→B/B→C projection either completed directly or converged under the same operation-key retry to C custody with exactly two handoff events. The related accessory/relay/API regression then passed `111` tests with the four opt-in cases skipped after cleanup. The container was removed; no production schema, provider endpoint or printer was used. This is real row-lock evidence for the relay slice, not completion evidence for the still-open unified 8.12 cross-surface matrix.
- Default reconciliation now has a concrete `legacy.double_count` boundary collector instead of a free-form supplemental test value. Its independently produced machine evidence is bound to the exact manifest, source snapshot, implementation and migration bundle, and separately carries negative-matrix digests plus mandatory zero authority counts for legacy quantity, child-rental, global-provider and shipment-writer sources. Non-zero authority, candidate drift or a waivable/wrong-scope requirement is rejected with the stable collection failure before phase completion. The collector accepts no database URL, provider client or printer adapter. Policy/collection/phase-adapter regression passed `37` tests; this closes the reusable collector implementation, while the representative-snapshot 12-key run remains open.
- A complete resolved-backfill composition passed on two disposable physical MySQL 8.0.46 instances: one tenant database and one control database, both named exactly `inventory_management_test`. It first committed the default warehouse/device assignment, canonical express type, planned logistics, reviewed structured address and one legacy-device logical unit, then injected a process crash before integration metadata. A direct post-crash observation proved the committed tenant facts survived while the control integration count remained zero. Re-entry with the same phase key replayed those five steps, added three metadata-only integrations and the strict empty-history verifier, then nine fixed SQL collectors, the real fleet schema observer, two schema collectors and the four-source legacy-authority evidence covered all 12 policy keys. An immediate second full replay produced the same reconciliation/result digest, three integrations and zero credential revisions. The tenant mutation account remained schema-only; schema inventory used a separate observer account on the same disposable tenant server because MySQL requires global `SHOW_ROUTINE` plus explicit schema metadata visibility. No production connection, provider or printer was used, and both containers were removed.
- That real run exposed two MySQL 8.0.46 compatibility defects and one test-composition transaction pitfall. `INFORMATION_SCHEMA.PARTITIONS` no longer exposes `ENGINE` and returns an empty `NODEGROUP` for ordinary tables, so the fixed inventory query now uses fields actually present on the supported server and the canonical schema-inventory identity is version 3; table engine remains covered by `INFORMATION_SCHEMA.TABLES`. MySQL returns `DECIMAL` for `SUM(BIGINT)`, so rental minor-unit aggregation now casts the final aggregate to `BIGINT`. A diagnostic read performed before backfill had also opened a repeatable-read snapshot and was removed so reconciliation starts only after mutation steps commit. Observer/fleet/schema-collector regression passed `72` tests, the complete MySQL composition passed `1`, and the expanded default-migration/fleet regression passed `313` with four opt-in cases skipped after their separate real runs.
- A separate synthetic portability run passed on one disposable MariaDB 10.11 source and one disposable MySQL 8.0.46 target. The source used `utf8mb3_general_ci` and three fixed tables containing Chinese text, decimal values, microsecond timestamps, a foreign key and an index. The runner invoked the source container's matching `mariadb-dump` under a schema-only `SELECT/SHOW VIEW` identity, supplied its option material over stdin, required single-transaction/no-lock/no-trigger/no-routine output, removed only the exact MariaDB sandbox header, and rejected database selection/creation, grants, users, file SQL, definers and any remaining MariaDB directive before restore. A schema-only target identity restored the dump and converted the database, every table and every character column to `utf8mb4_0900_ai_ci`; row counts and typed sample values remained equal. The opt-in integration test passed `1` case in `0.57s`; both tmpfs containers and all temporary credentials were then removed. This proves the fixed synthetic dump/restore path, not representative non-empty backfill or production-scale migration.
- Relay external-stage projection now has a durable, provider-free sink between the Core shipment ledger and the existing whole-chain handoff transaction. A `shipped` command is accepted only when its exact SF shipment is `submitted`, the successor rental/waybill and binding/credential revisions match, and exactly one succeeded create-waybill attempt owns the same response digest; strict control-job parsing binds the tenant access version, job type, payload, resource and idempotency identity, while the tenant status audit binds the source digest and operation key. A stable submitted shipment is the narrow custody-transition authority, but any in-flight/unknown/review provider or print fact still blocks the handoff. Exact replay creates no second event; a different digest conflicts and rolls back; durable conflicts become review outcomes without crossing a provider or print boundary. The focused relay/accessory/shipping/HTTP matrix passed `126` tests with five opt-in MySQL cases skipped. On a fresh tmpfs MySQL 8 container the expanded contention module passed all `5` cases, including two simultaneous projections of the same committed shipment converging to one handoff; the narrowed freeze check was then independently rerun for that case and passed `1` again. Every real-database write used a generated account limited to `inventory_management_test.*`; both containers were removed, and no production schema/provider/printer was used. This closes the committed create-waybill-result-to-handoff sink, not the still-missing producer/outbox installation, independently persisted delivery-result authority, or the unified 8.12 matrix.
- The same provider-free relay capability now registers a 30-second per-tenant periodic reconciliation job beside the exact per-shipment projection job. Shared scheduler gating binds current tenant access authority; the strict job contract binds scheduler origin, resource, payload and canonical timezone. Each routed tenant transaction discovers at most the oldest agreed relay whose successor owns a submitted SF shipment and succeeded create-waybill attempt, derives the exact shipment/waybill/revision response-digest command from those committed rows, and reuses the same projector; no candidate is a stable no-op and any inconsistent ledger enters review. A first real MySQL race exposed that the candidate discovery query can establish an older repeatable-read snapshot before a competing worker commits its audit; changing the final digest-audit verification to a locking current-read preserved the immutable identity while making the committed audit visible. The direct rerun then passed, and the complete disposable MySQL contention module passed all `5` cases in `3.70s`, with two reconciliation workers converging to one handoff. The expanded relay/accessory/shipping/shared-job regression passed `156` tests with five opt-in cases skipped after their separate real run. All temporary MySQL accounts remained scoped to `inventory_management_test.*`, all containers were removed, and no production schema/provider/printer was used. Immediate shipping-result enqueueing, deployment composition of the shared job process and independently persisted delivery-result authority remain open, so public `shipped/completed` stays fail closed.
- Default migration now has offline canonical bundle evidence rather than a caller-entered bundle digest. A fixed selector covers 188 migration/runtime/model/harness/test files and binds each normalized repository-relative path, size and SHA-256 together with the unique control and tenant Alembic heads. Source-baseline/preflight manifest templates can no longer supply the source snapshot digest, either schema head or the migration bundle digest; strict evidence injects those identities. Verified expand runs the source preflight first and then rejects bundle/head drift before either DDL verifier. The offline create/validate CLI accepts no DSN and an environment-injection test proves it does not read `DATABASE_URL`. Bundle/serde/phase targeted coverage passed `25` tests, the broader default-migration regression passed `200` with `5` opt-in cases skipped, and a fresh run of the complete expand composition passed `1` case in `25.19s` on four distinct tmpfs MySQL 8 targets. Those targets separated control/tenant qualification and apply; tenant DML and platform-read grants and foreign-schema denial were observed through exact accounts. All containers and random credentials were discarded, and no production instance, provider or printer was contacted. A fixed implementation commit/image and complete clean-environment migration/rollback reproduction remain open, so task 12.8 is not complete.
- Relay completion can no longer be authorized by an arbitrary caller digest. A new provider-free tracking ledger accepts only normalized `in_transit`, `delivered` or `exception` observations, locks the exact submitted shipment and waybill in a caller-owned tenant transaction, and writes a canonical digest plus safe technical facts to the existing tenant audit ledger. Exact digest lookup is bounded and strict-key parsed. The `completed` projector now requires exactly one persisted `delivered` observation whose digest, waybill and UTC occurrence time match the command; absence, malformed evidence or a different result enters durable review without changing relay custody/status. Focused relay/shipping coverage passed `139` tests with the five opt-in MySQL cases skipped. The complete disposable-MySQL contention module then passed all `5` cases in `2.99s`, including two simultaneous writes of one delivery observation converging to one canonical audit row. The generated account had authority only on `inventory_management_test.*`; the tmpfs container was removed and no production schema, provider or printer was used. Provider-backed tracking-job persistence and actual provider-worker invocation remain open.
- The create-waybill success path now has a concrete post-commit notification boundary. In the provider-result tenant transaction, a strict signal capture current-locks the exact succeeded attempt, submitted shipment and single agreed incoming relay, verifies the credential/binding revisions and derives only shipment/edge/waybill/result-digest/time facts; ordinary shipments produce no signal. After tenant commit, a separate enqueuer current-locks the active control tenant and uses its current access version and timezone to write the already-implemented direct projection job idempotently. A crash between those databases is intentionally not treated as atomic: the 30-second reconciliation now reuses the same signal derivation and recovers from the committed tenant ledger without another provider call. The broadened relay/shipping/shared-job run passed `181` tests with five opt-in MySQL cases skipped, and the refactored disposable-MySQL contention module passed all `5` in `2.86s`. The actual SF provider worker still needs to invoke this hook and the production shared-job launcher is not composed, so public external-stage HTTP remains fail closed.
- SF create-waybill now has a provider-fenced durable worker rather than only a committed-result sink. Its strict job identity binds current tenant access, shipment and attempt UUIDs and uses one attempt only; three current-authority checks surround exact credential preparation, the tenant `provider_submitting` commit, one typed provider dispatch and final result persistence. Unknown results are review-only and never auto-resubmitted; a known success remains terminal if direct relay enqueue loses its response, because periodic reconciliation derives the same signal from the committed tenant ledger. An explicit environment-free builder composes SF create, direct relay projection and 30-second relay reconciliation into one worker/process and registers only reconciliation as a schedule; construction opened zero database connections and made zero provider calls. The focused fake-provider chain passed `23` tests, including prepared shipment through direct job and provider-free custody handoff; the broader shipping/accessory/shared-job regression passed `202`. A fresh tmpfs MySQL 8 run passed all `5` contention cases in `3.17s`; its SF case used the real tenant store with a fake provider, deliberately lost the direct-enqueue response, and two concurrent reconciliation transactions converged to one handoff. The account had authority only on `inventory_management_test.*`, the container was removed, and no production schema/provider/printer was used. Real SF create/query adapters, unknown-result query reconciliation, the tenant-intent-to-control-job cross-database gap and a production executable launcher remain open, so public provider stages stay fail closed.
- SF uncertain-result recovery now has a concrete one-shot query path. A provider-order query request resolves the same immutable integration/account credential revisions and warehouse binding as the original shipment but carries no sender or receiver snapshot; credentials remain consume-once and redacted. The tenant store locking-selects only the oldest `create_waybill` attempt still at `provider_submitting` or `unknown`, verifies it against the exact shipment revisions, and before any query monotonically claims it as `needs_review`; a second worker therefore cannot repeat the automatic query. Three current-authority checks surround claim, exact credential preparation, the typed query and final persistence. `confirmed_success`, `confirmed_no_effect` and `still_unknown` reuse the existing reconciliation CAS; success emits the same optional relay signal, while still-unknown remains terminal manual review. The SF capability now registers four handlers in the shared process—create, unknown query, direct relay projection and relay reconciliation—and only the two 30-second reconciliation schedules. Focused query/create/ledger/credential/relay coverage passed `85` tests; the expanded shipping/accessory/shared-job run passed `209`. A new disposable tmpfs MySQL 8 run passed all `6` contention cases in `2.36s`: two concurrent query reconcilers competed for a crash-after-submit attempt, exactly one obtained the claim, a fake confirmed-success result updated the original attempt, and custody moved exactly once. The generated account remained limited to `inventory_management_test.*`; the container was removed, and no production schema, real provider or printer was used. The concrete SF network create/query adapter, tenant-intent-to-control-job persistent provenance/producer, public HTTP and production launcher remain open.

- The SF tenant-intent-to-control-job gap now has persistent, replayable provenance. Tenant head `20260823_shipping_intent` adds all-or-none actor/access/request provenance, a unique preallocated background-job UUID, enqueue acknowledgement time and a candidate scan index to each create attempt while retaining legacy attempts with no provenance. `ControlJobService` accepts that exact UUID and rejects an idempotency replay that supplies a different one. A provider-free 30-second handler discovers only current-access prepared intents, commits the exact control job in a separate transaction and then acknowledges the tenant row; loss after the control commit therefore replays one job rather than inventing actor/access facts or calling SF. The SF capability now contributes create, intent enqueue and unknown-query handlers plus three schedules to the same five-handler SF/relay process. Focused queue/provenance/composition coverage passed `72` tests, and the expanded shipping/accessory/shared-job matrix passed `256`. A fresh tmpfs MySQL 8 run initially exposed an actual 1213 deadlock in the previously implemented unknown-query candidate claim after the new scan index changed the query plan; both unknown-query and intent discovery now use `FOR UPDATE SKIP LOCKED`, after which all `7` real-MySQL contention cases passed in `2.44s`, including a single-winner intent acknowledgement race. Default-migration coverage passed `175`, tenant offline migration/bundle coverage passed `12`, and the canonical bundle now contains `190` files with tenant head `20260823_shipping_intent`. Every real write used a generated account limited to `inventory_management_test.*` in a disposable container; the container was removed and no production instance, real provider or printer was contacted. Trusted HTTP/batch intent creation, concrete SF network create/query adapters and the production launcher remain open.
- Superseding the preceding HTTP/snapshot gap, tenant head `20260823_shipping_contract` adds non-null immutable UTC scheduled-dispatch and server-derived single-package cargo snapshots to every outbound shipment, including deterministic legacy backfill. Both facts now participate in the request digest and travel through the tenant store, worker snapshot and consume-once SF credential request; no provider adapter was invoked. `/api/shipping-batch/schedule` is now an explicit SaaS runtime in the atomic shared graph. It authorizes `rental.ship + customer.pii.read`, rejects client routing/account/sender overrides by exact-key parsing, de-duplicates at most 100 rentals in first-seen order, derives stable shipment/job UUIDs from the client request UUID and commits all tenant intents before any control enqueue. Direct enqueue and tenant acknowledgement are recoverable optimizations over the same 30-second producer; an exact response-loss replay reused one shipment, attempt and job while a changed scheduled time conflicted. Focused shipping/provider/composition/route coverage passed `95` tests, tenant migration/head coverage passed `14`, and the current canonical bundle selects `191` files at control head `202608220029`. No production database, provider, printer, account or grant was touched. A disposable MySQL rerun, concrete SF network allowlist adapter and production launcher remain open.

- D65/D66 now provide one explicit existing-test-database launcher instead of per-run account/container orchestration. `make test-real-db` derives the exact `inventory_management_test` DSN in memory from the local `.env`, disables xdist, enables both real-database and global-DBA test opt-ins, and never prints the credential. The connection guard re-checks the selected schema, current role and grants, holds one advisory lock for every metadata lifecycle, and rejects schema switching, database/schema/account/grant/file/plugin statements and explicit cross-schema writes. Guard coverage passed `75` tests.
- The current MariaDB 10.11 test-schema matrix passed 84 distinct real-database cases: `7` accessory/shipping/intent/relay concurrency cases, `76` warehouse/Gantt/relay/API cases, and `1` tenant migration baseline→head→baseline→head round trip with ORM metadata equality. The migration case was rerun once after adding exact Alembic marker cleanup and passed again. The local full backend suite passed `2876` tests with `16` explicit opt-in skips. The canonical migration bundle still selects 191 files at control head `202608220029` and tenant head `20260823_shipping_contract`; its current digest is `3939fabc52373dc54d5fd9bc920d25c34d95eaf51ab0877f68219ed2b45db255`. No production business schema, provider or printer was used.
- D66 intentionally makes `inventory_management_test` destructive test infrastructure. The approved metadata rebuild replaced the previous application tables, teardown removed them, the migration cleanup removed `alembic_version`, and a final read-only inventory reported `remaining_table_count=0`. The earlier 9-table/6,264-row source snapshot and its digest remain historical observation evidence only; they no longer identify the current test-schema contents. A future non-empty representative migration must restore an approved snapshot into this same test schema and create a fresh source identity/manifest. Nothing was automatically copied back from the production business schema.
- The single-schema launcher now includes a control-plane base→head→base→head migration round trip with ORM metadata equality in the same existing-database migration module as the tenant round trip. It still creates no database or account and cannot select any schema other than `inventory_management_test`. The first rerun attempt on 2026-08-23 stopped before SQL because the configured endpoint `192.168.50.132:33601` refused the TCP connection; this is not recorded as passing database evidence and the earlier 84-case result remains the last successful real-database matrix. The new migration test, the exact five-phase default-migration registry builder and the existing phase adapters passed a local `136`-test regression; the two opt-in existing-database tests were separately collected and skipped without a test URL.
- Superseding 8.12 evidence is one version-locked `saas-core-relay-matrix.v1` command rather than a collection of independently quoted subsets. It requires control/tenant heads `202608220029` and `20260823_shipping_contract`, prints the current canonical bundle digest without exposing a DSN, and fails fast in dependency order. At bundle `f0469fe1f34a9cdb9cff3b4a0eb6d239776e6350c17b7dc53bce0f5532fa1a1b`, the backend slice passed `58` tests covering two warehouses, 100 devices, 31 days, explicit zero-day logistics, create/edit, accessory allocation, fixed booking/Gantt SQL budgets, warehouse movement and complete relay recomputation/handoff; the desktop HTTP fanout slice passed `53` tests across five files; the final D66 slice passed `10` real MariaDB contention tests on the existing `inventory_management_test`. No production business schema, real provider or printer was addressed. The earlier lines calling the complete 8.12 matrix open are retained as historical progress records and are superseded by this result.
- D67 supersedes that matrix backend split without discarding its historical result. Every selector that creates SQLAlchemy sessions, persists rows, or proves SQL/transaction behavior now runs serially on the one existing `inventory_management_test`; the local backend contains only database-free route/state-policy checks, while frontend tests remain database-free. Reusable module-scoped metadata lifecycles plus guarded row clearing replace per-case DDL, and combined control/tenant tests copy the two non-overlapping metadata sets into the same schema while retaining independent Sessions. At current bundle `0542dd882dffbec234a873c833f5fdc7e6800297735f96b746546f41743f4bf7`, `make test-saas-core-relay-matrix` passed local backend `17`, frontend `53`, and real MariaDB `69` tests in `567.32s`. The MariaDB slice proves booking availability remains five statements, Gantt remains eight tenant statements, full warehouse/accessory relay behavior, fake-SF-to-direct-handoff, ten row-lock/contention cases, and two batch-intent persistence/replay cases. It created no database/account/grant, contacted no production business schema/provider/printer, and the unregistered `/api/sf-test/*` route plus legacy batch-handler negative matrix prevents a direct provider/print bypass.
- The superseding D67 default database command also completed cleanly on the current code: `make test-real-db` passed all `144` selected cases in `1359.74s`. It includes tenant `baseline→head→baseline→head` and control `base→head→base→head`, both with ORM metadata equality, followed by booking/Gantt SQL budgets, `36` warehouse cases, `18` accessory-chain cases, the fake-SF direct handoff, `10` real contention cases, `73` relay/API cases, and `2` batch-intent cases. Module-scoped metadata plus guarded row clearing reduced the 73-case slice from an interrupted 38-minute pre-optimization run to `211.01s`. MariaDB correctly rejected the former fixture's impossible `source_relay_case_id=999`; the fixture now creates a real relay case, proves that its persisted authority blocks deletion, and explicitly removes it only after the chain-recalculation assertion. All lifecycle teardown completed inside `inventory_management_test`; no production business schema/provider/printer was addressed.
- The 2026-08-24 D67 follow-up removed every successful SQLite engine from the unit and integration test tree. The only remaining SQLite URL is an unconnected write-guard rejection input. Gantt HTTP, rental HTTP, registration publication, expand verifier identity/marker persistence, and tenant/control migration tests now either use the one guarded `inventory_management_test` schema or stay database-free when they only compose fake receipts. The former four-physical-database expand test is now a selector contract proving that old precondition is absent from the default command. A shared MariaDB batch passed `744` cases in `594.44s`; focused Gantt, qualification, default-tenant identity and registration replays also passed after fixing persisted timestamp precision by refreshing the inserted identity before producing its first digest. The full control `base→head→base→head` path passed in `564.50s`. The new 66-case per-revision migration group is structurally wired but not yet complete: an intentionally interrupted first run reported `2 passed, 3 failed` and exposed SQLite-specific assertions around MySQL `BINARY(32)`, implicit `RESTRICT`, and MariaDB CHECK exception mapping. The account-mutation round-trip and constraint cases and the backup-ack round-trip and constraint cases subsequently passed individually after backend-neutral assertion fixes. Until every remaining selector and then the full default command pass, D67 remains open. Every write was confined to `inventory_management_test`; no production business schema, provider, printer, database/account/grant creation, or second test schema was used.
- Superseding the preceding migration-count/progress statement, the current runner collects exactly `55` migration-group selectors, not 66. Every one now has a passing result from ordered `-x` MariaDB batches or a focused retry after its first failure. The real backend exposed and the tests now model: reflected JSON values require serialized JSON text; MariaDB may normalize `length` to `octet_length` and include identifier quoting; fixed `BINARY(32)` pads short inputs; revision `202608220020` intentionally precedes the `BINARY`→`VARBINARY` forward fix in `202608230038`; server-generated lease observation time must define the test window; and suspension authority fixtures must create their tenant/admin/TOTP/session parents before exercising action constraints. No confirmed product rule or migration was changed. Teardown completed with `remaining_table_count=0`. D67 remains open only for one current, uninterrupted default command containing isolated, migration and shared groups.
- After those migration fixes, the current shared-database group completed in one run with `761 passed, 1793 warnings in 633.03s`. The warnings are existing dateutil and SQLAlchemy UTC-construction deprecations; no test failed. OpenSpec strict validation also passed. The database lifecycle remained confined to `inventory_management_test` and subsequent teardown is verified separately before handoff.
- Closing D67, one uninterrupted `make test-real-db` invocation completed with exit code 0 on 2026-08-24. Its three sequential subprocesses reported isolated `326 passed in 3362.79s`, migration `55 passed in 9872.00s`, and shared `761 passed in 647.74s`, for `1142 passed` in total. The unusually long `2:44:31` duration belongs to the complete 55-case migration group, whose cases repeatedly reset the schema and exercise Alembic revision upgrade/downgrade paths; it is not the duration of one test. The command reused only the exact approved `inventory_management_test` database and did not call a real provider or printer.

## Remaining blockers and risks

- Production deployment still needs to supply the explicitly approved typed database-instance registry, bounded pool/cache settings and platform root-key directory. `create_app()` now installs the migrated shared/Gantt/Rental/Inspection/Warehouse/Relay bundle only behind that explicit typed enablement; no concrete production values or deployment configuration were invented in this local worktree.
- Rental detail/list/search/pending-return, booking bootstrap, booking availability, edit-context, final create/final update, ordinary status transitions, safe delete, tenant inspection receipt, and normalized Gantt range reads are migrated onto tenant runtimes with trusted tenant-local business dates. Both Gantt clients use the aggregate range contract; both create and edit clients use aggregate availability plus final locked mutation contracts. Availability accepts bounded requested accessory-type IDs and returns each type's realtime `requested`, `travels_with_device`, `fulfilled`, `relay_confirmation_required`, `shortage`, and `display_hint` facts without unit IDs; a held predecessor unit is surfaced as a non-blocking relay-confirmation path, while an edit's own link/holder facts are not counted as a foreign conflict. It still has no trusted SF duration-query adapter. Ordinary status dispatch owns holder/event changes and completion fails closed while custody remains; normal or damaged inspection receipt clears that custody, while missing receipt keeps the rental from completing. Safe delete is deliberately limited to rentals with no logistics, relay, or custody history that would make deletion ambiguous. Same-device agreed-chain recomputation, pre-handoff downgrade, exact-holder handoff, provider-free external-stage projection and D41 cross-warehouse inspection repair are implemented on caller-owned tenant transactions. External-stage HTTP exposure, Xianyu/SF outbox orchestration and tracking refresh remain pending and fail closed; the new projection is an internal sink for that future durable result, not a bypass. Final rental update still rejects relay-sourced or requestless travel-link edits that require a chain operation outside its ordinary mutation boundary.
- Credential validation now records known results through the fenced outbox and moves unknown results to explicit reconciliation. Historical tracking now has bounded exact-revision planning, a trusted tenant HTTP/runtime, typed result projection and a fake-tested production-shaped SDK adapter; real SF readiness, measured timeout/cache policy and production failure qualification remain open. SF create-waybill has an exact-snapshot execution ledger, provider-submission fence, typed result persistence, one-shot unknown query reconciliation and relay gap recovery, but still lacks the concrete network create/query adapter, tenant-intent persistent provenance/producer, public HTTP/cross-database coordinator and production launcher. Cancellation, printing and other provider paths still need the same complete treatment for response loss or a failed final recheck.
- Superseding the preceding SF blocker, tenant-intent persistent provenance and the provider-free exact-UUID producer are now implemented and tested. The remaining SF gaps are trusted public HTTP/batch creation, concrete network create/query adapters, production process configuration/launcher, cancellation and printing parity.
- Superseding that blocker again, trusted public batch creation and its exact HTTP replay contract are now implemented. The remaining SF gaps are the concrete network create/query allowlist adapter, official-estimate integration, production process configuration/launcher, cancellation and printing parity, and real-provider readiness evidence.
- The generic default-migration runner, ordered phase execution, exact five-phase registry, boundary-aware complete command, explicit-connection Alembic qualification/forward-only apply, D60 subscription grant writer, in-place identity/control registration and publication, default warehouse/device, express-type, planned-logistics, structured-address, logical-accessory and integration-metadata backfills, their concrete Session phase adapters, application/database-jobs/contract evidence adapters, the versioned reconciliation policy, concrete core SQL registry and schema observation collectors are implemented. The full expand phase passes on four physical MySQL 8 instances, including control/tenant migrations, tenant identity, control registration, replay and exact tenant-account grants. The version-locked source preflight is mandatory before verified expand and proven through the eligible test-schema read identity on the production instance. It now proves that the representative test snapshot is non-empty and binds the exact six-category boundary, so a zero-history or generic placeholder adapter cannot be selected. Remaining gaps are the genuinely approved non-empty historical shipment/print snapshot adapter, actual legacy-authority reconciliation observation, the full command against the representative test snapshot and its reconciliation. Historical shipment/print rows still expose the unresolved rule boundary: D61 forbids turning legacy SF/Kuaimai credentials into revisions, while the snapshot model expects a credential-revision identity; binding historical rows to a later rotated revision could change historical semantics and therefore was not invented locally. Task 12.5 is complete; 12.2–12.4 and 12.6 remain open only at their listed remaining boundaries.
- Historical MySQL 8 tenant migration and contention evidence remains available from the earlier disposable instances, but D66 supersedes that infrastructure for current work. The production-instance `inventory_management_test` was subsequently used as the approved destructive MariaDB test target and is now empty after verified teardown; the former nine-table source digest cannot authorize a new migration run. Current tests may explicitly reuse the global DBA only with the D65/D66 guard, while production Web/worker/backup/tenant routes still may not. MySQL 8-only physical-identity and MariaDB→MySQL compatibility claims therefore remain historical/risk items rather than current-bundle completion evidence.
- NAS retention code is complete. A read-only deployment review confirms the current Compose is a legacy single-database stack and cannot be used unchanged for a NAS SaaS environment: it lacks the separate worker/control-plane wiring, still declares Redis/provider environment inputs, and has inconsistent app/Nginx ports. Local NAS remains suitable as a non-production integration/DR host after a separate Compose is designed; if promoted to production it needs a different off-device backup failure domain. Real scheduled pull/cloud sync, off-host restore, real SMS/SF/Kuaimai/Xianyu readiness, root-key disaster recovery, deployment configuration, credential rotation under D61, and production-scale rehearsal remain pending.
- The frontend production build still reports a large desktop chunk warning.
- No commit or push was performed.
