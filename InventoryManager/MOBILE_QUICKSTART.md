# 移动端前端快速开始指南

## 5 分钟快速启动

### 前置要求

- Node.js >= 18.x
- npm >= 9.x
- 后端服务运行在 `http://localhost:5000`

### 1. 安装依赖

```bash
cd frontend-mobile
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问: http://localhost:5174

### 3. 使用浏览器的移动设备模式

1. 打开 Chrome DevTools (F12)
2. 点击设备工具栏图标 (Ctrl+Shift+M)
3. 选择移动设备(如 iPhone 12 Pro)
4. 刷新页面

## 功能导航

### 设备档期页面 (/)
- 查看所有设备的租赁情况
- 使用时间轴直观展示
- 点击租赁块查看详情
- 搜索设备或客户

### 预约页面 (/booking)
- 选择设备
- 选择日期范围
- 填写客户信息
- 提交预约

## 开发技巧

### 热重载
修改代码后自动刷新浏览器,无需手动刷新。

### API 代理
开发模式下,所有 `/api/*` 请求会自动代理到 `http://localhost:5000`。

### 移动端调试
使用 Chrome Remote Debugging 在真实设备上调试:
1. 在 `vite.config.ts` 中设置 `server.host: '0.0.0.0'`
2. 手机和电脑连接同一 WiFi
3. 访问 `http://<电脑IP>:5174`

## 常用命令

```bash
# 开发模式
npm run dev

# 生产构建
npm run build

# 预览构建
npm run preview

# 运行测试
npm run test

# 类型检查
npm run type-check
```

## 项目结构速览

```
src/
├── api/          # API 调用
├── stores/       # 状态管理
├── views/        # 页面组件
├── types/        # 类型定义
├── utils/        # 工具函数
└── router/       # 路由配置
```

## 下一步

- 阅读完整文档: `frontend-mobile/README.md`
- 查看技术选型: `docs/mobile-frontend-research.md`
- 部署指南: `docs/mobile-frontend-deployment.md`
- 实现总结: `docs/mobile-frontend-implementation-summary.md`

## 遇到问题?

1. 确认后端服务是否运行:
   ```bash
   curl http://localhost:5000/api/devices
   ```

2. 清除缓存重新安装:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

3. 查看浏览器控制台错误信息

---

祝开发愉快! 🎉
