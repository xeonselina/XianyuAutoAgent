# API 后台生产依赖（不含 dev/test/playwright/pandas 等）
# 对应 Dockerfile.api 使用

# Web Framework
fastapi==0.108.0
uvicorn[standard]==0.25.0
gunicorn==21.2.0
pydantic==2.5.0
pydantic-settings==2.1.0

# AI / LLM
openai==1.65.5

# Vector DB
chromadb==1.4.0

# Storage
redis==5.0.1
pymysql==1.1.0
cryptography==41.0.7

# HTTP / WebSocket
websockets==13.1
requests==2.32.3
httpx>=0.27.0,<1
aiohttp>=3.9.0

# Utilities
loguru>=0.7.0
python-dotenv==1.0.1
tenacity==8.2.3

# DingTalk Stream
dingtalk-stream>=0.24.0

# XianYu 签名 / 协议解析（cv-cat/XianYuApis 子模块依赖）
# 运行前需确保容器内已安装 Node.js >= 18（execjs 运行时）
# 注意：blackboxprotobuf 已用 xianyu_provider/upstream/blackboxprotobuf.py stub 替代，
#       不在此安装，避免其 protobuf==3.10.0 与 chromadb 的 protobuf>=3.13 冲突。
PyExecJS>=1.5.1
